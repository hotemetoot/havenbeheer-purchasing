# MVP Roadmap

| MVP | Scope | Status |
|---|---|---|
| 001 | 3-stage approval (dept → procurement → director), approve/return/reject | ✓ complete |
| 002 | Cancel by submitter (draft only) | ⊘ retired 2026-07-04 by D68 — draft stage dropped, so submitter-cancel removed; a PR starts at Pending Dept Approval |
| 003 | Quotation fields + currency + manual FX rate (D22) + formula USD total | ✓ complete |
| 004 | Manual `needs_director_approval` checkbox (D23) + routing | ✓ complete |
| 005 | Guard A — PR immutability when terminal (bulk-update limitation deferred, D24) | ✓ complete |
| 006 | Submitter-role routing variants (resolved via ACL under D25, no new build) | ✓ complete |
| 007 | Suppliers collection + supplier on PR (issues/evaluations descoped, D26) | ✓ built |
| 008 | Comments + attachments + soft fields | ✓ built (comments UI block removed post-verify; data layer kept) |
| 009a | PO collection + po_lines + Generate-PO button | ✓ built (D27 descoped po_lines pricing; planned 9a.4 total-maintenance cancelled) |
| 009b | PO send + budget zones (110%) + zone 2/3 notifications | ✓ built (D28 cancel→close collapse; zone-2 verified; zone-2 in-app notifications deferred — gated on Finance `main_approver`) — **superseded 2026-06-14 by D46**: Send + budget zones retired, replaced by the Issue-gated PO lifecycle (see D46 in decisions.md) |
| 009c | PO receiving (per-line `received_quantity`) | ✓ built + verified 2026-06-07 (Receive Guard `mhfp4d15uee` + Receiving recompute `ork27v016yo`; receiving UI user-built; R1–R4 passed) |
| 009d | PO completion / closing + immutability (no cancel — D28/D33) | ✓ built + verified 2026-06-09 (Complete `qh7b3hc5q1r` w/ D34 invoice+USD gate, broadened Close `f8gpu17s6hq`, D35 Close Guard `b6brl8r9c58`, PO/line immutability guards; C1–C5 + D34a/b + D35 all passed via API-staged fixtures) |
| 009e | PO template printing (Word → PDF) | ✓ built + verified 2026-06-09 (Carbone template `printingTemplates:kkooshlz8rf` → `po-template-mvp9e.docx`, Havenbeheer-branded; Print button `c579329db0d`; P1/P2 confirmed on PO-26-0006; source in `templates/`) |
| 010 | Optional submitter-chosen skip of dept-head approval (FYI notify + view access, D29) | ⊘ superseded by 012 (D36) — skip retired |
| 011 | Mandatory board approval at ≥ $15k USD after director — `pending_board_approval` + required signed-doc upload (D32) | ✓ built + verified |
| 012 | Submitter-selectable dept-stage approver (reassign to dept member, FYI-notify dept head; retires skip, D36) | ✓ built + verified 2026-06-09 (A1–A7 passed) |
| 013 | Regular-purchase flag — procurement-set `is_regular` bypasses director below $300; sub-$300 default flips to director; retires `needs_director_approval` routing (D37) | ✓ built + verified 2026-06-09 (workflow `cv237r8h7k9` rev `369161752084480` active; condition `bizoy1sj87j` swapped; is_regular editable on procurement form + read-only on popup; A1–A7 passed) |
| 014 | Projects & budget drawdown — USD budget envelope approved via its own ladder; project-linked PRs skip director+board (Procurement final) but keep dept-owner approval and are hard-blocked from exceeding `budget_usd` (D49, reverses D5) | ✓ complete 2026-07-18 — built+CLI-verified: `projects` collection, `purchase_requests.project` m2o, committed_usd recompute A/B, PR budget guards; **014.4 drawdown branch** on PR Approval (rev `372368060514304`, +cond `492iwdlv0mr`/update `48myq8dpqza`/notif `kykl9gnqj9h`); **014.2b/c** Project Approval `hzykothf9cx` **ENABLED** w/ 4 approver surfaces + initiator (centralized) + ACL (operations/procurement/director). **014.5 DONE 2026-07-18** — Complete Project button built by Alexander; PRJ-26-0079 completed in-app. **014.6 user E2E walked 2026-07-18, "so far so good"** (no failures reported; cases not individually ticked in docs/go-live.md §1.2). **D78 (2026-07-16):** committed_usd is now written at approval inside PR Approval (recompute A `j2fp3wa4o1k` disabled; delete recompute revised for formula refresh); first-approval regression in tests/plan.yaml. See [chunks/014-projects-and-budget-drawdown.md](chunks/014-projects-and-budget-drawdown.md) |
| 015 | "PR approved" notifications — on final approval, in-app notify the requester always + head of procurement only on the director path (not when Procurement is final, not on board path) (D50) | ✓ built 2026-06-24, **recipient rule superseded by 017/D88 on 2026-07-19** — workflow `cv237r8h7k9` rev `371864957943808` active; 3 notification nodes (`hy95mz4oo5f`/`dproua9530i`/`p53qqltz9v2`); A1–A5 passed. See [completed/015-pr-approved-notifications.md](completed/015-pr-approved-notifications.md) |
| 016 | Budget-safe PO line-item import — bulk-import PO lines; budget cap enforced at the Issue gate (Σ line_total ≤ PR.quoted_total) since import bypasses the D47 per-line guards; import button hidden unless PO is `draft` (D52) | ◑ Part A built + user-verified 2026-06-27 — `issue_po` rev `372351365087232` (+4 nodes `kkk684uupcd`/`hksnw304p3b`/`0udjhd90ljj`/`3ba6bu5e6un`); import action + hide rule user-built in UI. **D83 (2026-07-18):** the parked lines_total recompute gap is fixed — new async recompute `1ugka88lngm` reloads the line post-commit (old sync `5ukanitoy74` disabled); pending one UI import re-test. See [chunks/016-po-line-import-budget-safe.md](chunks/016-po-line-import-budget-safe.md) |
| 017 | Complete approval notifications — on every final approve/reject of a PR **or** a project, in-app notify submitter + submitter's dept head + procurement head; the person who decided is never notified, and procurement is skipped when the record never reached them. Returns stay silent (already in the to-do list). Supersedes D50's recipient rule; also fixes 3 mistitled notification nodes. | ◑ **built + suite-green 2026-07-19, pending user UI check** — PR Approval now `376242882347008` (47 nodes: 5 edits, 3 retitles, 5 new rejection nodes) and Project Approval now `376252879470592` (28 nodes, 6 new); predecessors disabled, suite 94/94 (D88). Over-budget auto-rejection notifies all three, per Alexander. **The revision copy was unfaithful both times** — it dropped the whole `committed_usd` write node from PR Approval, blanked both descriptions, and rewrote three Project Approval nodes including the Director stage's return-to-Procurement pointer; all restored and re-diffed. Remaining: the §1.2b walkthrough (reject at each stage, driving real approver users). See [completed/017-approval-notifications-complete.md](completed/017-approval-notifications-complete.md) |
| 018 | Stranded project commitment when a PO is closed — a cancelled project PO leaves its purchase request's money committed forever, with deleting the PR as the only way out. Fix: the **director raises the project budget**; no automatic release (auto-release would let procurement approve, close, and re-approve against the same envelope unseen — D85). Work is permission + audit: an approved project is currently fully locked, so the director can't raise the budget at all. | ○ **to do, low priority** — scoped 2026-07-18 from PO-26-0468, rare in practice, not urgent. See [chunks/018-stranded-project-commitment.md](chunks/018-stranded-project-commitment.md) |
| 019 | PO is execution only — lock the PR-copied terms at create, freeze the lines at issue. Procurement can no longer set supplier/currency/FX/total/issue-date when creating a PO, and once an order leaves **Draft** the only change its lines accept is recording receiving (D87) | ◑ **built + suite-green 2026-07-18, pending user UI check** — ACL create row `373257175367682` trimmed; new guard `eiscjvwiqr6` live as version `376238895661056` (narrow first version `376235116593152` disabled). Shipped **wider than drafted**: the drafted "block quantity/price" test could not see a *blanking* (an absent field and an emptied one look identical to NocoBase), so it was inverted to "allow only a plain receiving entry" — found by a test case written to probe it. Test rules R44/R45, suite 94/94. Phase 1b (remove the Add new PO button) was done by Alexander himself. See [chunks/019-po-execution-lock.md](chunks/019-po-execution-lock.md) |
| 020 | Receiving integrity + print gating — an order with nothing received returns to **Issued** instead of sticking at Partially Received; a negative received quantity is refused (over-delivery stays allowed on purpose); a printed order that isn't Issued carries a "DRAFT — NOT A VALID ORDER" watermark, since print is a non-CRUD action no guard or ACL can reach | ○ **to do, scoped 2026-07-19** — all three found by the first `nb-explore` exploratory session while the area's runner rules were green (they test paths; these bugs live in sequences). Touches receiving recompute `ork27v016yo` and Receive guard `mhfp4d15uee` (both need revisions), plus the Carbone template `printingTemplates:kkooshlz8rf`. See [chunks/020-receiving-integrity-and-print-gating.md](chunks/020-receiving-integrity-and-print-gating.md) |
| 021 | Guard payload-shape audit — a guard that reads a linked record straight from the request only understands the one way the screens write it; sent the other legal way it crashes and returns a server error instead of a refusal. Two PO-line guards were hit; purchase requests and projects have not been looked at (D89) | ○ **to do, before go-live** — scoped 2026-07-19 as the leftover from D89. `polncreateg1` and `8u81nd3vxhc` fixed and covered by R46; the other four `po_lines` guards checked clean. Everything outside `po_lines` is unaudited, and the PR guards read associations, which is the shape that breaks. See [chunks/021-guard-payload-shape-audit.md](chunks/021-guard-payload-shape-audit.md) |
| — | Record history on purchase orders | idea, not scoped (2026-07-18) — NocoBase ships a record-history plugin that gives a per-record timeline of who changed what and when. PRs and projects don't need it: their approval ladder already is that timeline, and they can't be edited once approved. A PO changes repeatedly after issue (receipts per line, payment status, invoice details) with no equivalent trail, so a history timeline would show who received what and when payment status moved. Check first whether the plugin is in this NocoBase build and what it costs in storage per tracked collection. |
| — | supplier_issues + supplier_evaluations | deferred (D26) — see [chunks/deferred-supplier-issues-evaluations.md](chunks/deferred-supplier-issues-evaluations.md) |

Detail lives in `chunks/NNN-*.md` for pending/active MVPs and `completed/NNN-*.md` for finished ones.
