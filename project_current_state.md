# Current Build State

**Last edit: 2026-06-25 (PR Duplicate button — user-added, doc-only).** The user added a **Duplicate** record action to the PR detail popup `2b367dbd157` using the **"Duplicate to form"** mode, shown only when a PR is `approved` or `rejected` (final states). It opens a pre-filled create form for a new PR — for repeat purchases, or to re-apply a rejected PR after edits. No collection/field/workflow/ACL changes (built-in `action-duplicate` plugin; m2o fields copied as references, system/unique/sequence fields auto-excluded). See the **Duplicate button** entry in the UI section + **D51**. Button uid not captured (no CLI read path for nested flowModels actions).

**Prior (2026-06-24 — demo-prep data wipe).** Cleared all transactional records ahead of a live end-user demo: **7 purchase_requests + 4 purchase_orders + 12 po_lines** deleted; approval-plugin records cascaded with the PRs (**6 approvals + 7 approvalRecords + 7 approvalExecutions** → all 0). Procedure: backed everything up to `backups/*-20260624-democleanup.json`, temporarily disabled the 3 destroy-blocking immutability guards (`496ookqmg01` PR / `xvcsdv07c5j` PO / `f3dkb37te22` PO-line), deleted in dependency order (lines → POs → PRs), then re-enabled all 3 (verified `enabled:true, current:true`). Other interception guards (budget/create/receive) left untouched — create/update-only, don't block deletes. **Sequence counters NOT reset** (per user) — next PR = `PR-26-0043`. No collection/field/workflow/ACL changes; no D-entry (routine wipe, mirrors the 2026-06-12/13 procedure).

**Prior (2026-06-24 — PO line-items total block — RunJS).** Added a JavaScript block at the bottom of the PO popup's **Line Items tab** that shows the running **sum of `po_lines.line_total` + PO currency**, refreshing on line add/edit/delete. Reactive `MultiRecordResource`; refresh wired by the user on the line-form Submits. No live config beyond the user-placed block. Canonical code committed at [snippets/po-line-items-total.js](snippets/po-line-items-total.js). Established (and saved to auto-memory) that **v2 has no native table summary footer** — the JS block is the chosen path. Details in the PO surfaces section below. No workflow/collection/ACL changes.

**Last verified:** 2026-06-24 (**MVP015 — "PR approved" notifications: BUILT + USER-VERIFIED (A1–A5 passed 2026-06-24). D50.** Added 3 in-app notification nodes to **PR Approval `cv237r8h7k9`** so that on `status→approved` the **requester** (`createdBy`) is always notified and the **head of procurement** (Pat, via root query `yrl9kgkrb3x`) is notified **only on the director path**. **Same-key revision `370060903907328` → `371864957943808`** (enabled+current; predecessor disabled; 31→**34 nodes**). New notification nodes: **`hy95mz4oo5f`** (downstream of `jy1365pvsce` Procurement-no-director → requester only), **`dproua9530i`** (downstream of `kj1zcmujub8` Director → requester + procurement head), **`p53qqltz9v2`** (downstream of `8gqeq6djrfj` Board → requester only). All: channel `approval-todo-in-app-message` (only configured channel — email deferred, needs SMTP), title `PR approved: {{$context.data.title}}`, content `{{{$context.data.pr_number}}} "{{$context.data.title}}" has been approved.`, `ignoreFail:true`, deep-link to PR view. **New approval surface uids on this revision** (regenerated by the revision): trigger `kag2bc9wrq7`/`uxw4q4agtye`, Procurement `li2jfcxa8dl`/`bpta19xtqk2`, Dept `a54gfhbawdy`/`hcpbe6rn93u`, Custom `qehdzrghvic`/`prs357bu9ih`, Director `80b19uw01we`/`82g9tfjkmj7`, Board `m3ajd5fxxt5`/`42dyyh3m3sg`. **VERIFIED 2026-06-24 (A1–A5):** requester-only on regular<$300 + board paths, requester+Pat on director path, all approval forms render their customizations (slim board form, is_regular checkbox) with no `flowModels:save` 403. **NB the active PR-Approval version is now `371864957943808`** (supersedes the doc-recorded `370060903907328`); the Active-workflows section below is STALE on this point. See D50 + [chunks/015-pr-approved-notifications.md](chunks/015-pr-approved-notifications.md).)

**Prior (2026-06-23):** (**MVP014 — Projects & budget drawdown: PARTIAL build (014.1 + 014.2a-logic + 014.3 done; surfaces / PR-revision / UI / verification pending). D49 (reverses D5).** **Built + CLI-verified:** new **`projects`** collection (USD budget envelope; `PRJ-YY-NNNN` sequence — own counter, working; 9-status enum mirroring PR + `closed`; `committed_usd` number default 0 workflow-maintained; `submitter`/`department` m2o; `approval_document` attachment) + **`purchase_requests.project`** m2o (FK `projectId`, **nullable** — regular PRs leave blank) + reverse o2m `projects.purchase_requests`. **committed_usd recompute:** workflow **A `j2fp3wa4o1k`** (id 371692444123136, collection sync, mode 3 create/update, changed=[projectId,quoted_total,fx_rate_to_usd,status]) + **B `0mckvnf319y`** (id 371692523814912, mode 4 delete) — both: guard `projectId != null` → aggregate **sum `quoted_total_usd`** (filter `project.id == projectId` AND status `$notIn [rejected,cancelled]`) → update `projects.committed_usd`. Self-test passed: create→1000, update→2000, delete→0. **PR budget guards** (request-interception, global, sync): create **`lylobzvlh5p`** (id 371696063807488) + update **`ebq41ibq60r`** (id 371696332242944) — block linking/editing a PR that pushes Σ child-PR `quoted_total_usd` over `project.budget_usd` (USD-only, reserve-on-submit counting all non-rejected/cancelled children, **≤ budget allowed**) OR onto a non-`approved` project; both ENABLED. Create-guard reads `{{$context.params.values.project.id}}` (nested m2o payload — verified); computes the new PR's USD as `quoted_total / fx_rate_to_usd`; update-guard loads the PR (appends project), gates on has-project, coalesces partial edits, excludes self. CLI-verified C1/C2/C3/C5 + U1/U2/U3 incl dynamic remaining-budget message, exact-budget boundary (==budget passes), no false-block on regular PRs. **Project Approval workflow `hzykothf9cx`** (id **371693220069376**, type approval, collection projects, **22 nodes**) BUILT + tree-verified but **DISABLED — NO approval surfaces yet** (014.2b pending → cannot enable): trimmed PR-ladder clone Set-Pending-Dept → Query-Proc-Dept → submitter-is-dept-head? (skip) → Dept Owner → **Procurement** → Director (always) → Board ≥ $15k (reads `budget_usd`); data-driven assignees (D40: dept-owner from `createdBy.mainDepartment`, proc+board=Pat via Proc-dept query `ioqkg84ju36`, director via Director-dept query `ll6jdxrhn9h`); **drops** custom-approver (D36) + is_regular/$300 (D37). Key nodes: dept-cond `cv0hpoum7n3`, dept-approval `tkw661dvfjq`, procurement `ik1ixug5rrs`, director `su6sbibmoky`, board-cond `707avxxeaag`, board `vwuxv7zih9f`. **DOC-LAG CORRECTED:** live active **PR Approval is `370060903907328`** (key cv237r8h7k9; user revision 2026-06-15; trigger surface `jgnb1rb896d`/`3mnqkhqzxyt`) — doc below still records `369536223739904` (STALE; full refresh deferred to the 014.4 revision). **NB:** its `executed` counter reads **0 because the user pruned execution history**, NOT because it never ran — the version HAS executed, so 014.4 MUST be a same-key revision (not in-place). **Pending:** 014.2b/c (approval surfaces + per-action comment models + render-enabler/board-attachment ACL), 014.4 (PR-Approval drawdown skip-branch on `ec2h8cqal32` br=2 before `bizoy1sj87j` + add `project` to trigger appends), 014.5 (Projects page/forms/Close button + PR `project` picker + PR/projects ACL whitelists), 014.6 (user E2E A1–F2 + finalize docs). CLI self-test data was created + deleted (0 orphans); PRJ + PR sequence counters advanced (not rolled back). See D49.)

**Prior (2026-06-21):** (**D48 — USD auto-sets `fx_rate_to_usd=1` + hides the rate field on all three PR forms (built + USER-VERIFIED in UI).** When `quoted_currency=='USD'`, the FX-rate field is auto-set to 1 and hidden; when SRD/EUR it is shown + **required** (newly required — was optional). Implemented purely as **form linkage rules** (no workflow), on the three surfaces where currency is set: **create form** `e76c40c8c79` (grid `5c325101ecc`), **Procurement ProcessForm** (grid `x30gs70f47k`), **Dept Owner ProcessForm** (grid `pqflpr2l3u4`). PO side untouched (PO currency/fx is PR-copied + ACL-locked, D46). Per grid, `stepParams.eventSettings.linkageRules.value` carries rule "USD FX rate = 1 (hide)" (`quoted_currency $eq USD` → `linkageAssignField fx_rate_to_usd=1` **mode `assign`** + `linkageSetFieldProps hiddenReservedValue` + `notRequired`) and rule "Non-USD FX rate (show + require)" (`quoted_currency $or {SRD,EUR}` → `visible` + `required`). Two approval forms already had a user-built assign-1 rule; those were **extended in place**. **Gotchas (auto-memory `feedback_linkage_assign_field_mode`):** assign `mode:"default"` only sets initialValue (won't overwrite a typed value — fixed via `mode:"assign"`); `setFieldProps.fields` = field **ItemModel uid** (`b199fc86a08`/`evq2pdr51us`/`xgar21mj94z`), `assignField.targetPath` = field **name**; `hiddenReservedValue` hides but still submits the value. Authored via raw `flowModels` update. Backups `backups/linkage-{create,proc,dept}-*-20260621.json`. **Doc-lag corrected:** active **PR Approval** is now **`370060903907328`** (key `cv237r8h7k9`; user revision) — doc previously recorded `369536223739904` (stale). Live approval-surface uids: Procurement `3pyea7jjxu2`, Dept `gg1a3ad0rca`, Director `xhh1l8kvp4r`, Custom `q1jm92hetly`, Board `ffwi9gi2j4i`. See D48.)

**Prior (2026-06-14):** (**D47 — PO line pricing + per-line budget hard-block (core built + CLI-verified; UI pending).** Re-added PO line pricing (partly reverses D27) and a **per-line budget hard-block** keeping `Σ(po_lines.line_total) ≤ PR.quoted_total` in the **PR's own currency** (no USD/FX — PO currency is PR-locked, so same-currency compare; partly reverses D46). **New fields:** `po_lines.unit_price` (double), `po_lines.line_total` (formula.js `ROUND(qty*unit_price,2)`, persists to a real column → aggregatable), `purchase_orders.lines_total` (double, workflow-maintained sum — **separate** from the untouched invoice `total`), `purchase_orders.needs_reprint` (boolean default false, workflow-maintained). **New workflows:** recompute **A** `5ukanitoy74` (collection sync, mode 3 create+update, changed=[quantity_ordered,unit_price]) + **B** `pnvp0dtitum` (collection sync, mode 4 delete) — both recompute `lines_total` and set `needs_reprint=true` when status≠draft (two workflows because mode only accepts [1,2,4,3]); **budget create guard** `8u81nd3vxhc` (request-interception `po_lines:create`) and **budget update guard** `c9c14tyn876` (request-interception `po_lines:update`, skips received_quantity-only writes) — both reject over-ceiling with a dynamic PR-currency message; **`issue_po` revised** (new current version **`370019772465152`**, old `369914017284096` retired) — added all-lines-priced gate (`count(unit_price>0)==count(lines)`) + sets `needs_reprint=false` on issue. ACL: scrubbed `lines_total`/`needs_reprint` from procurement's PO create/update whitelists; `unit_price` auto-added to po_lines create/update. **CLI-verified (root):** formula compute, recompute A/B (create/update/delete), create-guard block(10400>10000 SRD)+allow incl. first-line null coalesce (`N()`), update-guard block(10800)+allow incl. partial-update coalesce(`IF(ISBLANK)`)+exclude-self, received_quantity skips guard while receiving advances status, needs_reprint flips on post-issue edit, interception stacking (terminal+budget guards coexist). **Pending user UI walkthrough:** issue_po all-priced gate (custom-action — headless-undrivable), needs_reprint indicator + "Mark reprinted" button, and print-template `unit_price`/`line_total`/`lines_total` columns. One labelled orphan test PR ("TEST budget guard", approved, no PO) left for session-end wipe. See D47.)

**Prior (2026-06-14):** (**D46 — PO Issue-gated rework (built + USER-VERIFIED E2E).** New PO status **`issued`** (geekblue, after `draft`) + stamp **`issued_at`**; `sent`/`confirmed` kept inert. New lifecycle `draft → issued → (partially_received) → received → completed`; **close from `{draft, issued, partially_received}`**. **Send retired:** the Send button `slybgc23q1i` was **repurposed into "Issue PO"** (rebound `send_po`→**`issue_po`**, retitled, icon FileDoneOutlined; linkage unchanged = hide unless `draft`+Procurement); `send_po` (367086330314752) **disabled** along with its budget zones. **New `issue_po` workflow** (custom-action, sync, id **369914017284096**, 8 nodes): guard `status==draft` + supplierId/deliveryAddressId/currency not-null + `total>0` + ≥1 line (`count({{$context.data.lines}})`, math.js) → `status=issued`, `issued_at=now`; else reject+message+end(-1). **Print button `c579329db0d`** gained a linkage hiding it unless `status ∈ {issued, partially_received, received, completed}` (template binding `kkooshlz8rf` intact) — Print is now the issued-only gate (you can't reach `issued` without passing the Issue guard). **Revisions:** Receive Guard `mhfp4d15uee` →**`369970105614336`** (adds `issued` to receivable set; +msg fix); Close PO `f8gpu17s6hq` `368791950131200`→**`369914944225280`** + Close Guard `b6brl8r9c58` →**`369970388729856`** (closeable = `{draft, issued, partially_received}`; +msg fix). **Stale "sent" rejection messages in both guards corrected to "issued"** (E2E finding — node edits 400 on executed versions; re-revisioned). **ACL:** removed `supplier`/`total`/`currency`/`fx_rate_to_usd`/`issued_at` from procurement's `purchase_orders` **update** whitelist (action `369481068642316`, now 12 fields) — procurement can't edit PR-copied fields; lines/delivery_address/supplier_note/internal_notes still editable. **Spike findings (auto-memory `feedback_noncrud_action_workflow_triggers`):** `templatePrint` can't be workflow-triggered (post-action=create/update only; global interception=CRUD-only) and streams-PDF-only (no attach) — hence the Issue-button gate + manual download/email, no auto-attach. Logic validated via `flow-nodes test` + readbacks, then **USER-VERIFIED E2E 2026-06-14** (Issue render/click incl. incomplete-reject, Print visibility, procurement ACL edit-block, receiving from issued, close from {draft,issued,partially_received}); user set the 4 fields `readPretty` on the PO form. Backups: `backups/{send,print}-button-*-20260614.json`, `backups/procurement-po-acl-actions-before-trim-20260614.json`.)

**Prior (2026-06-13):** (**ACL/guard cleanup + doc hygiene + full walkthrough verification.** **D43** — deleted the 5 inert ACL scopes (3,6,7,8,9) + their 8 orphaned `dataSourcesRolesResourcesActions` rows (all parents already gone; backup `backups/acl-scopes-3-6-9-DELETED-20260613.json`); 4 scopes remain (2,4,5,10). **D44** — guard execution retention: set `options.deleteExecutionOnStatus:[1]` (auto-delete resolved/success only; keep `-1` blocks + `-2` errors) on all 6 interception guards, pruned 71 resolved executions (32 remain). **Doc hygiene** — PR-Approval Nodes/surfaces tables refreshed to the live active `369536223739904` (31 nodes; trigger `nzmc8r0606m`/`3l119fdioqd`); Suppliers page UID `gbg516xq3ie` (route `366222348582912`) captured. **Manual walkthrough authored** (`tests/manual-workflow-walkthrough.md`) covering PR-approval routing + full PO lifecycle + ACL — **FULLY VERIFIED by the user** (A1–A12, B1–B16, C1–C5 all checked off). **D45** — new **PO Line Create guard** `polncreateg1` (request-interception, blocks `po_lines:create` on `completed`/`closed` PO via `{{$context.params.values.purchase_order}}`). The association add-line route is closed by setting the add-line form Submit `8373f6c1297` (form `fd22f7bcaed`) `assignFieldValues.purchase_order = {{ ctx.popup.record.id }}` — injects the parent FK so the guard can query it; verified server-side AND via the real form (block on terminal, allow on draft). New auto-memory `feedback_request_interception_assoc_create_no_source` + `feedback_acl_orphan_action_scope_rows`. **Data:** the user ran the walkthrough (PR-26-0021..0027, PO-26-0022/0024/0025), then a **fresh-slate wipe** cleared everything at session end — 7 PRs + 3 POs + 9 lines + cascaded approval records (7 approvals, 17 approvalRecords, 8 approvalExecutions) all 0; 3 immutability guards toggled off→on (all 7 guards verified enabled). Backups `backups/*-20260613-clear.json`. `pr_number`/`po_number` counters NOT reset — next PR = `PR-26-0028`.)

**Prior (2026-06-13 — nested-picker fix + data wipe):** (**Nested-picker ACL fix completed + DATA WIPE.** Root-caused the *real* "Name already exists" on PO add-line (Pat only): the new Line Items tab add-line form (`purchase_orders.lines:create?updateAssociationValues[]=unit_of_measure`, CreateFormModel `fd22f7bcaed`) hit the server ACL middleware `checkChangesWithAssociation`, which prunes the nested UoM object to procurement's **update** field whitelist on `units_of_measure`, stripping `id` → link becomes duplicate-create. Root role bypasses the middleware (why admin worked). Fixed by adding `id` to the `units_of_measure` **update** whitelist (user) + `delivery_addresses` (actionId `366562898804749`) and `products` (`366562898804759`); `suppliers` already had full-field whitelist incl. `id`. Adding `id` to *create* alone does NOT fix it — the update branch is taken when the picked record exists. See auto-memory `feedback_acl_nested_association_id_whitelist`. **Orphan EditForm deleted:** the old PO-lines sub-table EditForm `01be4e39a26` (18-model subtree, incl. the broken-quick-create Unit select) was a `flowModels` orphan — present in the table but absent from the Line Items tab grid layout (`1d42bf8539c` lists only `fd22f7bcaed` + table `f3e868e9267`), so it never rendered. Backed up to `backups/orphan-editform-01be4e39a26-20260612.json`, all 18 models destroyed by uid. See auto-memory `feedback_flowmodels_orphan_not_rendered`. **DATA WIPE (fresh slate):** all 15 purchase_requests + 7 purchase_orders + 16 po_lines deleted (guards `496ookqmg01`/`xvcsdv07c5j`/`f3dkb37te22` temporarily disabled, then re-enabled+verified enabled=current). Approval-plugin records (15 approvals + 39 approvalRecords + 17 approvalExecutions) cascade-deleted with the PRs (no manual sweep needed — confirmed 0 remaining). Backups: `backups/{purchase_requests,purchase_orders,po_lines,approvals,approvalRecords,approvalExecutions}-all-20260612-cleanup.json`. **`pr_number` sequence NOT reset** — next new PR will be `PR-26-0021` (deleting records doesn't roll back the counter).)

**Prior (2026-06-12 late session):** (**D42 — Cancel retired + PO add-line fix.** User confirmed the D41 smoke test (PR submission healthy post-append-trim) and that the Board needs no Return action (Approve+Reject is final). "Name already exists" on PO add-line diagnosed: the add-line form's UoM select `d8c864bef73` had quick-create `quickAdd` — typed entries submit `{name}` without id → duplicate-create against the unique `name`; set `quickCreate:"none"` via raw flowModels patch (flow-surfaces configure reported success but didn't persist the key). **Second layer (same day):** the model's `props` carried persisted RUNTIME state from form-building (`allowCreate:true`, stuck `value`="each", `searchText`) which the client honors over stepParams — quick-create stayed active for Pat. Cleaned props (`allowCreate:false`, `quickCreate:"none"`, value/searchText nulled; flowModels updates deep-merge JSON, so explicit nulls required). Server path verified: line create with `unit_of_measure:{id}` links correctly (verify line created+deleted, UoM count unchanged 6). Users must hard-refresh to drop the cached model. `po_lines.titleField` → `description`. **Cancel fully retired (D42):** button `b197e8120a3` removed, both workflow keys `59ezifdoqvj` + `8yngslauuj4` deleted (all 6 versions), backups in `backups/cancel-*-20260612.json`. **ACL scope finding corrected:** `363334209503233` = built-in "All records" in `dataSourcesRolesResourcesScopes` (separate table from numbered `rolesResourcesScopes`) — NOT dangling, no fix needed; scopes 3/6–9 are referenced only by 8 orphaned action rows (parents deleted in role hardening; backup `backups/acl-scopes-3-6-9-orphans-20260612.json`). **Pending, permission-blocked this session:** scope 3/6–9 camelCase-fix-or-delete + orphan-row cleanup; guard executions retention (`deleteExecutionOnStatus:[1]` on the 6 interception guards) + pruning. Both need the user to run or approve next session.)

**Prior (2026-06-12):** (**ROLE HARDENING COMPLETE — Phases 2–4 closed, HANDOFF.md deleted.** Phase 2 final check passed: Pat board-approved PR-26-0019 ($100k) with a document upload — PR `approved`, approvalRecord status 2. **The user made their own PR Approval revision that evening: active is now `369536223739904`** (same key, 31 nodes, executed-from-0 at session start; all 6 approval surfaces regenerated AGAIN — see workflow section for new uids). On it the user rebuilt the Board ProcessForm slim: 4 readPretty fields (pr_number, status, title, description) + required editable `board_approval_document`, **and added a Reject action** (Approve `9rhk372rds3` / Reject `xxblb7503ws` comment models verified) — the earlier Approve-only question is resolved. **Phase 3 cleanup (D41):** dropped `purchase_requests.needs_director_approval` + `skip_dept_approval`, `purchase_orders.cancelled_at`, the whole secondary-approver fallback (`departments.secondary_approver` + users-side `department_secondary_approver_of` + both `secondaryApproverId` FK scalars) and `users.on_leave`; snapshots in `backups/*-20260612.json`. Removed `createdBy.mainDepartment.secondary_approver` from the PR Approval trigger appends (in-place on the unexecuted active version). Removed live UI nodes `p9acb4e35of` (PR popup), `2fbb3d11461` (departments secondary-approver column), `b1d31ddeafc` (users inverse column). ACL field whitelists were auto-scrubbed by the platform (view lists 37→35). Deleted the 8 MVP9d fixtures (PR-26-0008..0011, PO-26-T1..T4 + 2 lines) with guards temporarily disabled; all three guards re-enabled and re-verified blocking. Dropped the empty `test` collection. PR-26-0014 kept. Remaining low-priority follow-ups moved to "Notes for the next session".)

**Prior (2026-06-11 evening):** (**ROLE HARDENING Phase 2 DONE + user round-trip VERIFIED** (PR-26-0019 "gold bathtub", $100k: Dana got the Director task via the new query node, Pat got the Board task via qProc). **Board form fix:** Pat's Board approval popup showed mostly-blank fields — its PR fields sat in an `ApprovalDetailsModel` (approvalInformation), which renders the approval record's stored data (scalars + trigger appends only; associations like supplier/attachments BLANK; `quoted_total_usd` even had a DateTime display model). Rebuilt on the director-form pattern: 15 readPretty `PatternFormItemModel`s added to the ProcessForm grid `1y54nvue9eo` (live record data; order pr_number→…→justification, editable `board_approval_document` last; supplier needed `--title-field name` at add time), details block `kbuhnfiqvxj` REMOVED (backup: `backups/board-surface-r3w4l3ibkui-before-fix-20260611.json`). **Pat's board task on PR-26-0019 is still pending — final board approve (with the fixed form) not yet re-tested.** See auto-memory `feedback_approval_details_block_snapshot`. New test user **fiona.finance (14)**, Finance dept, member base + dept-inherited `finance` role, password `Test1234!`. `departments.main_approver` now fully populated: Director → Dana (12), Finance → fiona (14) (Procurement → Pat 11, Operations → Oliver 10 already set). `finance` role got the D38 render-enabler treatment: independent `purchase_requests` row (view all 37 fields + update 2 rejection fields @ scopeId 10) + member's 11 desktop routes (was zero). **PR Approval revisioned `369166026080256` → active `369495666917376`** (same key via raw `--body`; 31 nodes): new query node `ld6gei5ybc5` "Query Director Dept (for assignee)" inserted between `eg86l2ilhmk` and the Director approval node; Director assignees `[12]` → `{{$jobsMapByNodeKey.ld6gei5ybc5.main_approver.id}}` (filter-block form); Board assignees `[11]` → `{{$jobsMapByNodeKey.yrl9kgkrb3x.main_approver.id}}` (existing qProc). All 5 approval surfaces regenerated, all 10 referenced CommentFormModels verified to exist. Note: `flow-nodes test` is NOT implemented for `query` nodes — verified instead by live data readback + identical config shape to the proven qProc node. New surface uids: trigger `a88dhcc6p8k`/`yuli5bfnwfu`, Procurement `fjrw5u3cxxg`/`n7bjt8nz4uy`, Dept `h84ic5epwko`/`mjy73tr4cid`, Director `7qnayjp9lzl`/`k7ynav1jxw4`, Custom `wc4et0bt8si`/`qld8db015tw`, Board `r3w4l3ibkui`/`z1o8zfquxgn`. Board surface still Approve-only (pre-existing — confirm with user). Phases 3–4 remain in [HANDOFF.md](HANDOFF.md).)

**Prior (2026-06-11):** (**ROLE HARDENING phases 0–1 DONE + user-verified** — see the new "Roles & ACL" section: member/director strategies now view-only, member snippets reverted to vanilla, new dept-bound `operations` role (PR create/update + routes), procurement trimmed (no PR create/importXlsx, whitelisted updates), director render-enabler grant. D38. **Remaining phases 2–4 are specced in [HANDOFF.md](HANDOFF.md).** Also this session: doc-lag corrected — **PR Approval active is `369166026080256`** (user revision; doc said `369161752084480`), **Generate PO active is `368786562547712`** (6 nodes — the user *manually removed* the default-line create node; NOT the branch-drop bug). Four stray disabled workflows deleted (`close_po_draft` deprecated custom-action, `s6m4i5hrmzs` "Send PO copy", `jsgbxph9444`, `s4syz7vom4n`). **User flow change (D39): PR `draft` stage removed from the flow — users submit directly.** The status enum + `defaultValue: 'draft'` still exist on the field; the change is at form/flow level. Implications (cancel workflow guard checks `status=="draft"`; scope 2 references draft) tracked in HANDOFF.md.)

**Prior (2026-06-09):** (**MVP013 DONE + VERIFIED (A1–A7 passed)** — D37 regular-purchase flag: below $300 now defaults to Director, with a new Procurement-set `is_regular` checkbox as the opt-out (regular + <$300 → approved directly). `needs_director_approval` retired from routing (column kept, unused). New field `is_regular` (`fw0rz4we4bw`, boolean default false). **PR Approval `cv237r8h7k9` revisioned `369154168193024`→active `369161752084480`** (30 nodes): condition `bizoy1sj87j` is now basic-OR `is_regular != true` OR `quoted_total_usd >= 300` (the `needs_director_approval` leaf removed). Procurement ProcessForm grid `03uy1easu6l` gained editable `is_regular` (wrapper `dcbp2xasi5f`); detail popup grid `16975baef39` gained read-only `is_regular` (wrapper `6q8zzia1bt7`). flow-nodes test passed all 6 quadrants incl null-safety. **Doc-lag corrected:** the active version before MVP013 was a user revision `369154168193024`, NOT the doc-recorded `369076269481984` — both now stale. See D37. Prior: **MVP012 DONE + VERIFIED** — A1–A7 all passed via test users; user set the picker data scope (same-dept + exclude-self) in the form UI. Submitter-selectable dept-stage approver, retires the MVP010 skip (D36, supersedes D29). Two new `purchase_requests` fields: `use_custom_approver` (boolean), `custom_approver` (m2o → users, FK `customApproverId`). **PR Approval workflow `cv237r8h7k9` revisioned to active `369076269481984`** (30 nodes) — the skip branch was repurposed into a custom-approver branch: condition `eafkgfa3axi` retitled "Custom approver chosen?" = AND(`use_custom_approver==true`, `custom_approver.id != null`, `custom_approver.id != createdById` self-exclusion); br=1 → reused notify `5h232imw9ss` (FYI dept head) → new approval node `fifkfnqn9pm` "Custom Approver Approval" (assignee `{{$context.data.custom_approver.id}}`, approvalUid `1a1zhqfmpdl`/taskCardUid `369lxzap6i2`, duplicated from Dept Owner node so form+comment models identical) → outcome updates `r8f85w1w1ze`/`8loza7mypxd`/`vhize11vzsg`; br=0 → unchanged Dept Owner Approval `cfg687cye3n`. `custom_approver` added to trigger appends. Auto-skip `5hed96jh1u7` (submitter IS dept head) unchanged + still first. **Doc-lag corrected:** active PR-Approval before this was `368983543906304` (NOT `368641179582464` as previously recorded) — both now stale. **UI:** create form `e76c40c8c79` got checkbox `r9iu06my5ot` + picker `gfozpat06nw` + show/require linkage; detail popup `2b367dbd157` got both fields read-only (`7jjizc66ex2`/`9tpfw54yy1e`) and the skip read-only `in24ndj91et` was removed. **`skip_dept_approval` column RETAINED but dead** (no workflow reads/writes it; off both forms). Picker data scope (same-dept + exclude-self) was set by the user via the form "Set data scope" UI (public CLI can't author it); self-approval is also hard-blocked server-side. **Only remaining (low priority):** drop the dead `skip_dept_approval` column with explicit OK. See D36. Earlier same day: **MVP9e DONE + VERIFIED** — PO template printing: Carbone template `printingTemplates:kkooshlz8rf` → `storage/print-templates/po-template-mvp9e.docx` (Havenbeheer-branded), Print button `c579329db0d`; user confirmed live PDF on PO-26-0006, P2 internal_notes-not-printed holds. See the `printingTemplates` collection + MVP9e note in "Notes for the next session". Earlier same day: **MVP9d fully VERIFIED end-to-end** — all 8 cases C1/C2/C3/C4/C5 + D34a/D34b/D35 passed via Pat-driven UI clicks against API-staged fixtures; see "Notes for the next session". D34 — Complete PO requires an attached invoice + positive `total_usd`; guard revisioned `368746204954624`→`368971625791488`, Complete button `e60dce0bb2d` linkage extended. D35 — Close Guard request-interception `b6brl8r9c58` rejects + messages a Close attempt on a non-closeable (e.g. `received`) PO, which the post-action Close workflow couldn't.** See Complete/Close workflow sections + decisions D34/D35. Prior baseline below from 2026-06-07.) Earlier: live env queried via `nb api` — **MVP9d backend built (Complete workflow + broadened Close + PO/line immutability guards); UI buttons + end-to-end C1–C5 verification PENDING (user wires the buttons)**; MVP9c PO-receiving verified; D32/MVP011 board-approval ≥ $15k; PR/PO numbering D31; director-approval $300 floor D30; MVP9b Send-PO + Close-PO; MVP010 skip-dept-approval). Next: **MVP9d verification, then MVP9e — PO template printing (not started; a bare "Template print" button exists on the PO detail block but no template is configured or tested)**.

**MVP9d backend built 2026-06-07 (PO completion / closing / immutability) — UI + verification pending.** Scope aligned to **D28** (no `cancel` action, no `cancelled` status — "cancel a draft" is just close-with-reason, already in 9b). Four workflow changes: (1) **Complete PO** (custom-action, sync, key `qh7b3hc5q1r`, ver `368746204954624`): guard `complete_guard` (status==received) → true: `complete_update` status=completed + completed_at; false: reject msg + end(-1). (2) **Close PO** rebuilt as a **post-action event** (`action` type, sync, local mode, key `f8gpu17s6hq`, ver `368791950131200`): guard `close2_guard` (status ∈ {draft,sent,confirmed,partially_received}) → `close2_update` (status=closed, closed_at=now). **Correction:** the original 9d build wrongly made Close a `custom-action` (`close_po_draft`) — a form Submit can only bind pre/post-action/approval, so custom-action couldn't bind; the old one is disabled + renamed "(deprecated custom-action)". The user binds `f8gpu17s6hq` to the Close EditForm's Submit (local mode). (3) **Guard: PO Immutability** (request-interception, global, sync, key `xvcsdv07c5j`, ver `368747670863872`; actions update+destroy on `purchase_orders`): query target PO → condition OR status∈{completed,closed} → reject msg + end(-1). (4) **Guard: PO Line Immutability** (request-interception, global, sync, key `f3dkb37te22`, ver `368747750555648`; actions update+destroy on `po_lines`): query line (appends purchase_order) → condition OR purchase_order.status∈{completed,closed} → reject msg + end(-1). No new fields (completed_at/closed_at/close_reason/close_comment + status enum all pre-existed). **UI:** the user builds the Complete + Close buttons themselves (as with the 9c receiving UI); the doc-recorded Close button `lylrxwl1b3g` was **removed by the user** and is gone from the surface. **Caveat:** the PO immutability guard locks ALL updates on terminal POs, including payment fields — fine now (no Finance payment UI/workflow built, finance role strategy = null), but a future payment MVP will need a carve-out (cf. the `closed_for_new_pos` exemption pattern). See D33.

**Doc-lag corrected 2026-06-07 (live `nb api` re-query):** active versions had drifted from the doc — **Send PO** active is `367086330314752` (was recorded `366981771362304`); **PR Approval** active is `368641179582464` (was `367885604880384`); **PO Receiving recompute** active is `368710576439296` (was `368072534523904`). Sections below updated. A stray disabled PR-Approval ver `368062113775616` may also exist (not re-confirmed this session).

**MVP9c built + verified 2026-06-07 (PO receiving).** Receiving UI built by the user; R1–R4 passed (partial→partially_received; full→received + Pat notified; correct-down reverts received→partially_received; receiving on non-sent PO blocked). Two workflows: (1) **Guard: Receive** (request-interception, key `mhfp4d15uee`, ver `368072131870720`) blocks a `received_quantity` change on a PO not in `sent`/`confirmed`/`partially_received`/`received`; (2) **PO Receiving recompute** (collection trigger, key `ork27v016yo`, ver `368072534523904`) derives `po_lines.line_status` + PO header status on `received_quantity` change, and notifies Procurement (Pat) when fully received. Pricing untouched (D27 — no PO-total recompute). No new fields (received_quantity/line_status already existed from 9a; Record History already on via collection-level `logging:true`). No plan changes during build → no D-entry.

**D32 built + verified 2026-06-02 (MVP011):** mandatory board approval at `quoted_total_usd >= 15000`, *after* the director. New status `pending_board_approval`, new multi-attachment field `board_approval_document`. PR Approval revisioned `367158084370432` → **`367885604880384`** (key `cv237r8h7k9`); board branch hangs off the director-approve branch via condition `fro4hak78r9`, routing ≥ $15k to a 4th approval node **Board Approval** (`01upqmcb1qy`, assignee Pat) whose ProcessForm requires the signed-doc upload. Two follow-on fixes were needed to make a fresh approval form usable by a non-admin approver: (1) granted Procurement `create` on `attachments` (independent resource perm) so Pat can upload; (2) pre-created the per-action `CommentFormModel`s the blueprint omits (else approver 403 on `flowModels:save`). See decisions D32 and auto-memory `feedback_approver_attachment_upload_acl` + `feedback_approval_blueprint_comment_models`.

MVPs 1–8 built. MVP7 was reduced to suppliers-only (D26). MVP8 added comments collection + 4 soft fields + UI surfaces; the comments UI block in the PR detail popup was removed by the user post-verification (data layer remains — can be re-added). **MVP9a built 2026-05-26**: PO + po_lines + lookups, Generate-PO workflow (`2izsx8uv50r` v `366595041853440`), Create-PO Guard (`vgv8hcrtjvx`), Generate-PO button on PR surfaces with procurement-only visibility. PR↔PO relation re-shaped to clean m2o + virtual hasOne. **MVP9b built 2026-05-29**: PO `draft → sent` with budget-zone guard (Send PO workflow `send_po`), PO `draft → closed` (Close PO workflow `close_po_draft`), Send + Close buttons on PO surfaces. `cancelled` collapsed into `closed` (D28). Zone-2 in-app notifications (9b.3) deferred — gated on Finance dept `main_approver` being set (still NULL). **MVP010 built + verified 2026-05-29:** optional submitter `skip_dept_approval` (D29) — field + UI toggle live; PR Approval workflow revision `367150157135872` (key `cv237r8h7k9`) activated and end-to-end verified by user. When skipped, dept head gets an in-app FYI notification and the PR routes straight to Procurement. **D30 built + verified 2026-05-30:** mandatory director approval at **`quoted_total_usd >= 300`** — PR Approval revisioned to `367158084370432` (key `cv237r8h7k9`); the director-decision condition `bizoy1sj87j` is now an OR of the manual `needs_director_approval` checkbox and the $300 USD floor. Next: **MVP9c — receiving**.

This file is the **single source of truth** for the live NocoBase environment state. Update it at the end of every session that creates or modifies collections, fields, workflows, or UI surfaces. Commit changes alongside the build commits they describe.

---

## CLI environment

- **Env name:** `havenbeheer`
- **URL:** http://localhost:13000
- **NocoBase version:** 2.1.0-beta.36
- **CLI version:** 2.1.0-beta.33 (newer available — run `nb self update --yes`)
- **Auth:** OAuth token (valid, auto-refreshes)

---

## Collections

### `purchase_requests`

| Field | Interface | Notes |
|---|---|---|
| pr_number | sequence | **D31** — auto `PR-YY-NNNN` (2-digit year, 4-digit yearly-cycling counter, e.g. `PR-26-0004`); field key `jo6vvssc0i5`, `inputable: false`. Assigned at PR creation. PO derives its number from this (see Generate-PO workflow). **Counter note:** `PR-26-0001` was consumed by a since-deleted verification PR; deleting records does not roll back a sequence counter, so the next new PR will be **`PR-26-0002`**. |
| title | input | |
| description | textarea | |
| justification | vditor | rich text |
| submitter | m2o → users | |
| department | m2o → departments | |
| status | select | see values below |
| rejection_reason_category | select | |
| rejection_comment | textarea | |
| cancellation_reason | textarea | MVP2 — **orphaned since D42** (cancel retired; field kept for history) |
| cancelled_at | datetime | MVP2 — **orphaned since D42** (cancel retired; field kept for history) |
| quoted_total | number | MVP3 — entered by submitter or procurement |
| quoted_currency | radioGroup (USD/SRD/EUR) | MVP3 |
| fx_rate_to_usd | number | MVP3 — **entered manually** by submitter or procurement |
| quoted_total_usd | formula | MVP3 — formula.js: `{{quoted_total}} / {{fx_rate_to_usd}}`, auto-computes, read-only. **Division**: `fx_rate_to_usd` is local-per-USD (e.g. 35 SRD per 1 USD). |
| quotation_attachment | attachment (multi) | MVP3 |
| ~~needs_director_approval~~ | — | **DROPPED 2026-06-12 (D41)** — was MVP4, retired from routing by D37; values snapshotted. |
| is_regular | checkbox (boolean) | **D37 (MVP013)** — default false (real boolean); set by **Procurement** on their approval form. Below $300, `is_regular==true` → approved directly; else → director. Field key `fw0rz4we4bw`. Read by condition `bizoy1sj87j` via `{{$jobsMapByNodeKey.ec2h8cqal32.data.is_regular}}`. Editable on procurement ProcessForm, read-only on detail popup. |
| approved_at | datetime | MVP4 — written on final approval (both paths) |
| supplier | m2o → suppliers | MVP7 — optional "Suggested supplier" |
| expenditure_type | select (capex/opex/maintenance/consumables) | MVP8 |
| is_emergency | checkbox (boolean) | MVP8 — UI flag only, no routing impact |
| needed_by | dateOnly | MVP8 |
| other_attachments | attachment (multi) | MVP8 — distinct from `quotation_attachment` |
| comments | o2m → pr_comments | MVP8 — comment thread relation; UI block removed post-verify (data layer kept) |
| purchase_order | oho (hasOne) → purchase_orders | MVP9a — virtual inverse of `purchase_orders.purchase_request`. No FK column on PR side. |
| ~~skip_dept_approval~~ | — | **DROPPED 2026-06-12 (D41)** — was MVP010/D29, dead since MVP012/D36; values snapshotted. |
| use_custom_approver | checkbox (boolean) | **D36 (MVP012)** — default false; submitter opts to reassign the dept-approval stage. On the create form; read-only on detail popup. |
| custom_approver | m2o → users | **D36 (MVP012)** — FK `customApproverId`; the chosen dept-stage approver. Create-form picker (required when `use_custom_approver` on; data scope = same dept + exclude self, set via form UI). Read by the workflow's "Custom approver chosen?" branch. |
| board_approval_document | attachment (multi) | **D32 (MVP011)** — field key `ingr48ghuzj`, type belongsToMany → attachments. Editable+**required** on the Board Approval ProcessForm; read-only display on the PR detail popup. Holds the board's signed hard-copy scan. |
| project | m2o → projects | **D49 (MVP014)** — FK `projectId`, field key `nmyxhz2g36e`, **nullable** (regular PRs leave blank). Links a PR to an approved project budget envelope; reverse is `projects.purchase_requests`. When set + project approved, the PR skips director+board (014.4 drawdown branch) and is budget-guarded at submit. Not yet on the PR forms (014.5 pending). |

**`status` values:** `draft`, `pending_dept_approval`, `pending_purchasing_review`, `pending_director_approval`, `pending_board_approval` (**D32** — purple, after director), `info_requested`, `approved`, `rejected`, `cancelled`

### `projects` (MVP014 — built 2026-06-23, D49)

Budget-envelope collection. A project is approved once via its own ladder (Project Approval workflow,
disabled until surfaces land), then PRs link to it and draw against `budget_usd`. **titleField = `title`.**

| Field | Interface | Notes |
|---|---|---|
| project_number | sequence | `PRJ-YY-NNNN` (own yearly counter, key 39329), `inputable:false`. Mirrors `pr_number` (D31). |
| title | input | required (server validator), **title field** (referenced by `purchase_requests.project` m2o). |
| description | textarea | scope of work. |
| budget_usd | number (double) | USD envelope cap — the figure both budget guards check. |
| status | select | `draft`, `pending_dept_approval`, `pending_purchasing_review`, `pending_director_approval`, `pending_board_approval`, `info_requested`, `approved`, `rejected`, `closed` (cyan). Default `draft`. Colors mirror PR; `closed` replaces PR's `cancelled`. |
| submitter | m2o → users | FK `submitterId`, label `username`. Set by the approval root node from `{{$context.applicant.id}}`. |
| department | m2o → departments | FK `departmentId`, label `title`. Set from `{{$context.applicant.mainDepartmentId}}`. |
| approval_document | attachment (multi) | belongsToMany → attachments. Board sign-off (≥ $15k); editable+required on Board ProcessForm only (014.2b). |
| approved_at | datetime | written on final approval. |
| rejection_reason_category | select | 6 options reused verbatim from PR. |
| rejection_comment | textarea | |
| committed_usd | number (double) | default 0, **workflow-maintained** running Σ of non-rejected/cancelled child-PR `quoted_total_usd` (recompute A/B). Powers a "remaining = budget − committed" UI display without a relation-traversing formula. |
| purchase_requests | o2m → purchase_requests | reverse of `purchase_requests.project` (FK `projectId`). |

### `departments`

| Field | Notes |
|---|---|
| main_approver (m2o → users) | primary approval assignee |
| ~~secondary_approver~~ | **DROPPED 2026-06-12 (D41)** — fallback never built; users-side mirror `department_secondary_approver_of` + both `secondaryApproverId` FK scalars dropped too |

| Department | ID | main_approver |
|---|---|---|
| Procurement | 363554444476416 | Pat (user 11) |
| Director | 363554454962176 | Dana (user 12) — **D40** |
| Finance | 363554454962177 | Fiona (user 14) — **D40** |
| Operations | 363554454962178 | Oliver (user 10) |

### `users` — extra fields
- ~~`on_leave`~~ — **DROPPED 2026-06-12 (D41)**; no user-level extras remain (approver changes = edit `departments.main_approver`, D40)

### `suppliers` (MVP7 — built 2026-05-24)

Collection key `a4ogom91smz`. Fields:

| Field | Interface | Notes |
|---|---|---|
| name | input | required, title field |
| display_name | input | |
| tax_id | input | |
| email | email | |
| phone | input | |
| address | textarea | |
| country | select | |
| default_currency | select (USD/SRD/EUR) | |
| payment_terms | select (Net30/Net60/Net90/COD/Prepayment) | per D17 |
| status | select (active/inactive/blocked) | default `active` |
| current_rating | number (1–5) | manual per D6 |
| notes | textarea | |

### Collections NOT built (postponed — D26)
- `supplier_issues` — postponed from MVP7
- `supplier_evaluations` — postponed from MVP7

### `pr_comments` (MVP8 — built 2026-05-26)

Comment-template collection (`@nocobase/plugin-comments`). Baseline fields only: `id`, `content` (vditor, long text, not deletable), `createdAt`, `createdBy`, `updatedAt`, `updatedBy`. Linked to `purchase_requests` via the `purchase_requests.comments` o2m relation.

### `purchase_orders` (MVP9a — built 2026-05-26)

Header collection for purchase orders. Created via Generate-PO workflow from approved PRs (one PR → one PO per D9).

Key fields: `po_number` (**D31** — plain `string`/`input` field, key `esjop6vlpp9`; the old auto-`sequence` field was deleted by the user and replaced. Written by the Generate-PO workflow as the PR's `pr_number` with the `PR-` prefix swapped to `PO-`, e.g. `PO-26-0004`), `purchase_request` (m2o → purchase_requests, FK `purchaseRequestId`), `supplier` (m2o), `delivery_address` (m2o), `status` (default `draft`), `currency`, `fx_rate_to_usd`, `total` (workflow-maintained, no formula), `total_usd` (formula.js: `{{total}} / {{fx_rate_to_usd}}` — division, local-per-USD), `payment_status`, `payment_date`, `expected_delivery_date`, `invoice` (attachment), `attachments` (attachment multi), `supplier_note`, `internal_notes`, `budget_override_comment`, `close_reason`, `close_comment`, audit timestamps (`sent_at`, `confirmed_at`, `completed_at`, `closed_at`). **D47 added:** `lines_total` (double, key `7pzjnxgvhq3`, **workflow-maintained** sum of `po_lines.line_total` via recompute A/B — this is the figure the budget guards check; **distinct from `total`** which stays the supplier-invoice figure, titled "Invoice Total"), `needs_reprint` (boolean, key `6osy3b72f2s`, default false, workflow-maintained — set true on a post-issue line edit, cleared by `issue_po` and the "Mark reprinted" button). Both are removed from procurement's create/update field whitelists.

**`status` values (post-D46):** `draft`, **`issued`** (D46, geekblue, after draft), `sent`, `confirmed`, `partially_received`, `received`, `completed`, `closed`. `cancelled` was removed in MVP9b (D28 — collapsed into `closed`). **`sent`/`confirmed` are inert since D46** (Send retired — nothing produces them; kept for history). New lifecycle: `draft → issued → (partially_received) → received → completed`; **close from `{draft, issued, partially_received}`**. Two terminal states: `completed` (happy path) and `closed` (everything else, with a `close_reason`). New stamp `issued_at` (datetime) written by `issue_po`.
**`close_reason` values:** `no_longer_required`, `supplier_unable_to_fulfill`, `partial_fulfillment_accepted`, `duplicate`, `replaced_by_new_po`, `other`.
**Note:** the `cancelled_at` datetime field was **DROPPED 2026-06-12 (D41)** — it was unused (all values NULL) and no workflow wrote it. (`purchase_requests.cancelled_at` is unrelated and kept — the Cancel PR workflow writes it.)

### `po_lines` (MVP9a — built 2026-05-26)

Line items for purchase orders. Pricing was descoped 2026-05-26 (D27), then **line pricing was re-added 2026-06-14 (D47)** (the per-line budget hard-block needs it). Fields: `purchase_order` (m2o), `product` (m2o optional), `description` (textarea, required), `unit_of_measure` (m2o), `quantity_ordered`, `received_quantity`, `line_status` (default `pending`), **`unit_price`** (double, key `7lxc5x35fj5`, in PO currency; in procurement create/update whitelist), **`line_total`** (formula.js `ROUND({{quantity_ordered}} * {{unit_price}}, 2)`, key `vydunb4hvvp`, read-only — a same-record formula so it persists to a real column and is aggregatable). `line_total_usd` was NOT re-added (the budget check is same-currency, no USD needed). PO-level sum lives in `purchase_orders.lines_total`, maintained by recompute workflows A `5ukanitoy74` / B `pnvp0dtitum` (D47) — **this replaces the cancelled 9a.4 Total-maintenance workflow**, but writes `lines_total`, NOT the invoice `total`.

### `delivery_addresses`, `units_of_measure`, `products` (MVP9a — built 2026-05-26)

Lookup collections. `delivery_addresses` has `name` (title), `address`, `is_default`, `status`. `units_of_measure` has `name` (title, **unique**), `abbreviation`, `status`. `products` (v1 stub) has `name` (title, **unique**), `description`, `default_uom` (m2o → units_of_measure), `status`.

### `printingTemplates` (MVP9e — template-print registry)

Owned by `@nocobase/plugin-action-template-print`. One record per print template. Fields: `name` (the `templateName` the Print button references), `title`, `collectionName`, `rootDataType`, `dataSource`, `filename`.

| Record `name` | title | collectionName | filename | Notes |
|---|---|---|---|---|
| `kkooshlz8rf` | Purchase Order | purchase_orders | `po-template-mvp9e.docx` | MVP9e PO template (Carbone). Bound to Print button `c579329db0d`. Old stub `17809643261187811286095403871.docx` kept on disk for rollback. |

The `.docx` files live on disk at **`/Users/alexander/nocobase/storage/print-templates/`** (not in attachments). Repo source-of-truth: `templates/build_po_template.py` → `templates/purchase-order-template.docx` (+ `templates/main-logo.png`). To change: edit builder → run it → `cp` docx over the storage file (file overwrite, no live config write). Carbone markers: data root `d`; **select fields use `.label`**; assoc to po_lines is **`lines`**; **`internal_notes` is NOT rendered** (P2). See auto-memory `feedback_template_print_*`.

### Deleted collections
- `fx_rates` — deleted 2026-05-24 (MVP3 simplification under D22)

---

## Active workflows

### PR Approval workflow
- **Key:** `cv237r8h7k9`
- **⚠ DOC-LAG (2026-06-24):** the live active version is **`371864957943808`** (MVP015/D50 same-key revision; trigger surface `kag2bc9wrq7`/`uxw4q4agtye`; 34 nodes = 31 + 3 approval notification nodes `hy95mz4oo5f`/`dproua9530i`/`p53qqltz9v2`; surface uids regenerated — see the MVP015 status block at the top). Predecessor `370060903907328` is now disabled. The version/surface details below describe the long-stale `369536223739904`; they'll be refreshed when the next chunk revises the workflow. **A same-key revision is still mandatory** (this lineage has executed).
- **Active version ID:** `369536223739904` (enabled=true, current=true) — **a USER revision made 2026-06-12 ~02:50** (same key, 31 nodes, D40 structure carried: query node `ld6gei5ybc5` + variable assignees intact). The user rebuilt the **Board ProcessForm slim** on it: 4 readPretty fields (pr_number, status, title, description) + **required editable** `board_approval_document`, actions **Approve + Reject** (comment models `9rhk372rds3`/`xxblb7503ws`, both verified to exist — the Approve-only gap is closed; Return still absent, presumed intentional). **Trigger appends trimmed 2026-06-12 (D41):** `createdBy.mainDepartment.secondary_approver` removed (in-place config edit, version was executed=0) → appends now `createdBy`, `createdBy.mainDepartment`, `createdBy.mainDepartment.main_approver`, `custom_approver`. **Approval surfaces on THIS version:** trigger `nzmc8r0606m`/`3l119fdioqd`, Procurement `3pd230wckly`/`80msuchjg95`, Dept `36y3u2xqbpe`/`10z35kjn4ap`, Director `0k6lbm72b5h`/`esqpw2gulpp`, Custom `vnikugovtl2`/`u3msb9xon2t`, Board `9hzzn2kou79`/`175tfp1vxqp`. All 12 referenced comment models verified.
- **Prior active (now stale):** `369495666917376` — **D40 data-driven approvers, built + user-verified 2026-06-11 (Phase 2).** Assignee resolution proven live on PR-26-0019 (Dana ← query node, Pat ← qProc). **Board surface `r3w4l3ibkui` was rebuilt post-revision** (blank-fields fix): ProcessForm grid `1y54nvue9eo` now holds 15 readPretty PR fields + editable `board_approval_document` (wrapper `yvr0cusiwum`, required) last; the `ApprovalDetailsModel` was removed. Pat's board approve click on the fixed form still pending. 31 nodes: same-key revision of `369166026080256` + new query node **`ld6gei5ybc5`** "Query Director Dept (for assignee)" (filter id `363554454962176`, appends `main_approver`) inserted between `eg86l2ilhmk` and Director approval `sxvxwl498xg`. Director assignees = `{{$jobsMapByNodeKey.ld6gei5ybc5.main_approver.id}}`, Board (`01upqmcb1qy`) assignees = `{{$jobsMapByNodeKey.yrl9kgkrb3x.main_approver.id}}` (both filter-block form per `feedback_approval_assignee_from_variable`). Approval surfaces on THIS version (all regenerated, comment models verified): trigger `a88dhcc6p8k`/`yuli5bfnwfu`, Procurement `fjrw5u3cxxg`/`n7bjt8nz4uy`, Dept `h84ic5epwko`/`mjy73tr4cid`, Director `7qnayjp9lzl`/`k7ynav1jxw4`, Custom `wc4et0bt8si`/`qld8db015tw`, Board `r3w4l3ibkui`/`z1o8zfquxgn`. **Board surface has only an Approve action (no Reject/Return buttons)** though the workflow has board reject/return outcome nodes — confirm with user whether intentional.
- **Prior active (now stale):** `369166026080256` — a user revision found live 2026-06-11 (audit re-query); 30 nodes, hardcoded assignees Director [12] / Board [11]. Superseded by the D40 revision above. Its surfaces (Procurement `su56vx8p048`, Dept `nwjvklrknlf`, Director `7kuv49goy4o`, Custom `h2sh3r9monu`, Board `xwd019rr78k`) are stale. Disabled.
- **Prior active (now stale):** `369161752084480` — **D37/MVP013, built 2026-06-09**: the director-decision condition `bizoy1sj87j` (id `369161754181632`, retitled "Director required? (>= $300 OR not regular)") now reads the new `is_regular` flag. Basic-engine OR: `notEqual {{$jobsMapByNodeKey.ec2h8cqal32.data.is_regular}} != true` **OR** `gte {{$jobsMapByNodeKey.ec2h8cqal32.data.quoted_total_usd}} >= 300` (the `needs_director_approval == true` leaf was removed/retired). true→Director (`eg86l2ilhmk`), false→approved (`jy1365pvsce`). 30 nodes (no branches dropped). Revision of `369154168193024` (forced same key). **New Procurement approval surface uid (regenerated by this revision):** approvalUid `otlzkklrg0n`, taskCardUid `bhx9rxilzwm`; its ProcessForm grid `03uy1easu6l` gained editable `is_regular` (CheckboxFieldModel wrapper `dcbp2xasi5f`). All other MVP012 custom-approver nodes/surfaces unchanged (keys preserved). flow-nodes test passed; A1–A7 user verification pending.
- **Prior active (now stale):** `369154168193024` (a **user revision** discovered this session — the doc had wrongly listed `369076269481984` as active; `369076269481984` and all earlier are disabled). The MVP012 custom-approver node keys below (condition `eafkgfa3axi`, notify `5h232imw9ss`, approval `fifkfnqn9pm`, outcomes `r8f85w1w1ze`/`8loza7mypxd`/`vhize11vzsg`) are preserved across the MVP013 revision; surface IDs from earlier revisions are stale — refresh when next touched.
- **Prior active (now stale):** `368983543906304` (pre-MVP012). `368641179582464` was also superseded. Both disabled.
- **(Pre-MVP012 line, retained for history) was-recorded Active version ID:** `368641179582464` — **doc-lag corrected 2026-06-07** (was recorded `367885604880384`; a later user revision is now active). The D32 board-approval ≥ $15k branch was activated + verified 2026-06-02 (16,000 USD PR) on this lineage. *(The Nodes/surfaces tables below were refreshed 2026-06-13 against the live active version `369536223739904` — no longer describe `367885604880384`.)* All prior versions disabled — see "Stale IDs".
- **Prior version ID:** `367158084370432` (now disabled) — D30 revision (director $300 floor), was active 2026-05-30 → 2026-06-02.
- **Type:** approval, collection `purchase_requests`
- **Trigger appends (since D41, 2026-06-12):** `createdBy`, `createdBy.mainDepartment`, `createdBy.mainDepartment.main_approver`, `custom_approver`
- **Trigger approvalUid:** `nzmc8r0606m`; **taskCardUid:** `3l119fdioqd` (active version `369536223739904`). *Stale:* the D32-revision pair `2xxjrbc0b36`/`1yj5xm96k3p` is from `367885604880384` — do not use.
- **Nodes** (refreshed 2026-06-13 against active version `369536223739904`; **31 nodes**; node keys are preserved across revisions, surface uids are not):
  - Root update `1f6a1h52l9u` — "Set status to Pending Dept Approval"
  - Query `yrl9kgkrb3x` (qProc) — Procurement dept `363554444476416`, appends `main_approver` (= Pat). Feeds Procurement + Board assignees.
  - Condition `5hed96jh1u7` "Is submitter the dept main approver?" — basic `equal(createdById, createdBy.mainDepartment.mainApproverId)` → skip dept stage
    - br=1 (true): Update `nkbguc8uo7z` → pending_purchasing_review (skip dept, no notify)
    - br=0 (false): Condition `eafkgfa3axi` "Custom approver chosen?" — basic AND: `use_custom_approver == true` AND `custom_approver.id != null` AND `custom_approver.id != createdById` (self-exclusion) — **D36/MVP012** (this is the repurposed old MVP010 skip branch; `skip_dept_approval` is gone)
      - br=1 (true): Notification `5h232imw9ss` "Notify dept head — PR reassigned to custom approver" (FYI) → Approval `fifkfnqn9pm` **Custom Approver Approval** (assignee `{{$context.data.custom_approver.id}}`)
        - br=2 (approve): Update `r8f85w1w1ze` → pending_purchasing_review
        - br=1 (return): Update `8loza7mypxd` → info_requested
        - br=-1 (reject): Update `vhize11vzsg` → rejected
      - br=0 (false): Approval `cfg687cye3n` **Dept Owner Approval** (assignee `{{$context.data.createdBy.mainDepartment.mainApproverId}}`)
        - br=2 (approve): Update `xqlzgk0326f` → pending_purchasing_review
        - br=1 (return): Update `bm50djboga3` → info_requested
        - br=-1 (reject): Update `1b06nufq3bi` → rejected
  - Approval `ec2h8cqal32` **Procurement Approval** (main flow after the dept condition; assignee `{{$jobsMapByNodeKey.yrl9kgkrb3x.main_approver.id}}` = Pat)
    - br=2 (approve): Condition `bizoy1sj87j` "Director required? (>= $300 OR not regular)" — **OR group (D37/MVP013, basic engine):** `is_regular != true` (notEqual) **OR** `quoted_total_usd >= 300` (gte), both read from `{{$jobsMapByNodeKey.ec2h8cqal32.data.*}}`. `!= true` so null→Director (null-safe); no quote → `quoted_total_usd` is 0, never trips the floor. (D37 replaced the retired `needs_director_approval` leaf.)
      - br=1 (true): Update `eg86l2ilhmk` → pending_director_approval → Query `ld6gei5ybc5` "Query Director Dept" (dept `363554454962176`, appends `main_approver` = Dana) → Approval `sxvxwl498xg` **Director Approval** (assignee `{{$jobsMapByNodeKey.ld6gei5ybc5.main_approver.id}}` — D40 data-driven)
        - br=2 (approve): Condition `fro4hak78r9` "Board approval required? (>= $15k)" — basic `gte(quoted_total_usd, 15000)`, reading `{{$jobsMapByNodeKey.ec2h8cqal32.data.quoted_total_usd}}` (D32)
          - br=0 (false, < $15k): Update `kj1zcmujub8` → approved, approved_at=now
          - br=1 (true, ≥ $15k): Update `fm6kvldiduk` → pending_board_approval → Approval `01upqmcb1qy` **Board Approval** (assignee `{{$jobsMapByNodeKey.yrl9kgkrb3x.main_approver.id}}` = Pat, via qProc)
            - br=2 (approve): Update `8gqeq6djrfj` → approved, approved_at=now
            - br=1 (return): Update `s1tignlqc54` → info_requested
            - br=-1 (reject): Update `2rd8sap9m04` → rejected
        - br=1 (return): Update `z1x6ghkmr2t` → info_requested
        - br=-1 (reject): Update `t2odlgyqdra` → rejected
      - br=0 (false): Update `jy1365pvsce` → approved, approved_at=now (no director)
    - br=1 (return): Update `pket0lgmjyk` → info_requested
    - br=-1 (reject): Update `01vfxfgw6s3` → rejected
- **Notification node** `5h232imw9ss`: in-app channel, receivers `["{{$context.data.createdBy.mainDepartment.mainApproverId}}"]`, `ignoreFail=true` (FYI must not block flow). on_leave→secondary fallback NOT implemented (D29; secondary-approver fallback fully removed in D41).
- **Approval surfaces (active version `369536223739904`; all 12 referenced CommentFormModels verified to exist):**
  - Trigger: approvalUid `nzmc8r0606m`, taskCardUid `3l119fdioqd`
  - Dept approver (`cfg687cye3n`): approvalUid `36y3u2xqbpe`, taskCardUid `10z35kjn4ap`
  - Procurement (`ec2h8cqal32`): approvalUid `3pd230wckly`, taskCardUid `80msuchjg95`
  - Director (`sxvxwl498xg`): approvalUid `0k6lbm72b5h`, taskCardUid `esqpw2gulpp`
  - Custom approver (`fifkfnqn9pm`): approvalUid `vnikugovtl2`, taskCardUid `u3msb9xon2t`
  - **Board (`01upqmcb1qy`):** approvalUid `9hzzn2kou79`, taskCardUid `175tfp1vxqp`. User-rebuilt slim ProcessForm: 4 readPretty fields (pr_number, status, title, description) + **editable+required** `board_approval_document`. Actions: **Approve `9rhk372rds3` + Reject `xxblb7503ws`** (both comment models verified; no Return — presumed intentional).

The four MVP8 fields (`expenditure_type`, `is_emergency`, `needed_by`, `other_attachments`) are present on all three approval forms: editable on dept, read-only (`pattern: readPretty`) on procurement and director.

**Note:** Filter by key `cv237r8h7k9` + enabled=true to get current version.
**Revision how-to (CLI bug workaround):** `nb api workflow workflows revision` mints a NEW key (stray copy). To get a same-key revision, pass the key via raw `--body`: `revision --filter-by-tk <srcVersionId> --body '{"key":"cv237r8h7k9","enabled":false,"current":false}'`. See auto-memory `feedback_workflow_revision_key_bug`.

### Project Approval workflow (MVP014, D49) — DISABLED (no surfaces yet)
- **Key:** `hzykothf9cx` · **Version ID:** `371693220069376` · type `approval`, collection `projects`, async.
- **`enabled:false`** — built + tree-verified, but **cannot be enabled until 014.2b** builds the approval
  surfaces (initiator + 4 ProcessForms) + per-action comment models + ACL render/board-attachment grants.
- **Trigger appends:** `createdBy`, `createdBy.mainDepartment`, `createdBy.mainDepartment.main_approver`.
  approvalUid/taskCardUid not yet set (blueprint will set them in 014.2b).
- **22-node trimmed PR-ladder clone** (no custom-approver, no is_regular/$300; director always reviews):
  - `amc633b85wx` update "Set Pending Dept Approval" (also sets submitterId/departmentId from applicant)
  - `ioqkg84ju36` query Procurement Dept (appends main_approver = Pat) — feeds Procurement + Board assignees
  - `cv0hpoum7n3` condition "Is submitter the dept main approver?" → br1 skip update `oklnh22f6n4`; br0 approval `tkw661dvfjq` Dept Owner (assignee `{{$context.data.createdBy.mainDepartment.mainApproverId}}`, approve→`mtugzs9s6yq` purchasing review / return→`vh4g7bfgun9` info / reject→`ftt0dnnq8iw` rejected); converges →
  - `ik1ixug5rrs` approval **Procurement** (assignee `{{$jobsMapByNodeKey.ioqkg84ju36.main_approver.id}}`)
    - approve→`s8k21sgph93` pending_director → `ll6jdxrhn9h` query Director Dept → `su6sbibmoky` approval **Director** (assignee `{{$jobsMapByNodeKey.ll6jdxrhn9h.main_approver.id}}`)
      - approve→`707avxxeaag` condition "Board required? (≥$15k)" reading `{{$context.data.budget_usd}}` → br0 `yhc99ooprj8` approved+approved_at / br1 `7rkl02hfzej` pending_board → `vwuxv7zih9f` approval **Board** (assignee = Pat via Proc query; approve→`8fyj9j9tw2r` approved / reject→`22e5jxvgd90` rejected; no return)
      - return→`8iss65fo9k2` info / reject→`5j5hyd0piqi` rejected
    - return→`uyrqpqvybm2` info / reject→`8wywlcbea89` rejected
- All approval node assignees use the filter-block form (`feedback_approval_assignee_from_variable`).

### Project Commit recompute workflows (MVP014, D49) — ENABLED, CLI-verified
- **A `j2fp3wa4o1k`** (id 371692444123136) — collection sync, mode 3 (create+update), changed=[projectId, quoted_total, fx_rate_to_usd, status]. Nodes: condition `xj07gww8tbx` `projectId != null` → br1 aggregate `stf49k2lx11` (sum `quoted_total_usd` where `project.id==projectId` AND status `$notIn [rejected,cancelled]`) → update `5d63uatcqw3` `projects.committed_usd`.
- **B `0mckvnf319y`** (id 371692523814912) — collection sync, mode 4 (delete). Same node chain (cond `ujy3kkqgc3k` → aggregate `hsshy332wo1` → update `m4ljzirnlnb`). Two workflows because collection `mode` only accepts [1,2,4,3] (mirrors D47 recompute A/B).
- No loop risk (writes `projects`, trigger is on `purchase_requests`). Verified: create→1000, update→2000, delete→0.

### PR Budget guards (MVP014, D49) — ENABLED, CLI-verified
These **stack** with the existing PR immutability Guard A (`496ookqmg01`); both fire on `purchase_requests` update, first to `end(-1)` wins. Cap = `project.budget_usd` (USD-only; quoted_total_usd is already USD). Reserve-on-submit: counts all child PRs except rejected/cancelled; **≤ budget allowed**.
- **Create guard `lylobzvlh5p`** (id 371696063807488) — request-interception `create` on purchase_requests, global sync. Nodes: condition `tj2a9m2ew2c` `{{$context.params.values.project.id}} != null` → br1 query `ocqebs07roz` (project) → condition `uxnyyk6bfqk` `status != approved` → br1 reject "not open" + end(-1); main → aggregate `66fxlhss3ko` (sum sibling `quoted_total_usd`, excl rejected/cancelled) → calc `9rkf9kg5xss` (formula.js `N(sum) + values.quoted_total / values.fx_rate_to_usd`) → calc `p32wagxd2nn` (remaining = budget − N(sum)) → condition `agavq549jx2` (math.js `calc_new > q_proj.budget_usd`) → br1 reject (dynamic remaining-budget msg) + end(-1). Verified C1/C2/C3/C5.
- **Update guard `ebq41ibq60r`** (id 371696332242944) — request-interception `update`, global sync. Nodes: condition `fwfv6ho0vlq` (basic OR: values.quoted_total OR values.fx_rate_to_usd present — skips non-amount edits) → br1 query `kc762s9pa2d` (PR, appends project) → condition `3plajptadwj` `projectId != null` (else allow) → br1 condition `7au1b2xf29a` `project.status != approved` → br1 reject + end; main → calc `2iamw66ayyq`/`fmxne0x2rmk` (effective qty/fx coalesce) → aggregate `i4uayouh3va` (sum others, **excl self** `id != filterByTk`) → calc `dj27n84thia` (new total) → calc `zuylltvofo7` (remaining) → condition `9ah4yxvs1al` (math.js over budget) → br1 reject + end. Verified U1/U2/U3 incl no-false-block on regular PRs.

### ~~Cancel Purchase Request workflow~~ — RETIRED 2026-06-12 (D42)
- Both cancel workflows **DELETED** (all versions): Cancel Purchase Request (key `59ezifdoqvj`) and Cancel PR Guard (key `8yngslauuj4`). The D39 draft-stage removal made the guard (`status=="draft"`) permanently unmatchable. Cancel button `b197e8120a3` removed from the PR detail popup `2b367dbd157`. Backups: `backups/cancel-workflows-20260612.json`, `backups/cancel-button-b197e8120a3-20260612.json`. Fields `cancellation_reason`/`cancelled_at` + the `cancelled` status value KEPT (historical; nothing writes them).

### Guard A — PR immutability (MVP5)
- **Key:** `496ookqmg01`
- **Active version ID:** `366217145548800` (enabled=true)
- **Type:** request-interception, global, sync; actions: update + destroy on `purchase_requests`
- **Node chain:** Query (`q33wtlxitr1`) → Condition OR status∈{approved,rejected,cancelled} (`nbs3zmsr60x`) → branch 1: response-message + end(endStatus:-1)
- **Known limitation (D24):** does NOT intercept bulk update. See [decisions.md](decisions.md).

### Generate PO workflow (MVP9a; D31 PO-number derivation)
- **Key:** `2izsx8uv50r`
- **Active version ID:** `368786562547712` (enabled=true, current=true) — **user revision, 6 nodes: the default-line create node (`4p3q7oq3co5`) was removed manually by the user** (confirmed 2026-06-11 — intentional, not the suspected revision branch-drop bug). The node chain below otherwise holds (guard → query address → calc number → create PO → false-branch message+end).
- **Prior active (now stale):** `367255610327040` — **D31 revision: derive `po_number` from PR's `pr_number`, built + verified 2026-05-30.** Revision of `366626572533760` (which itself was a user revision after the doc previously recorded `366623590383616` as active — that and the other intermediate versions are now stale; see Stale IDs).
- **Type:** custom-action, sync, collection `purchase_requests`; appends `[supplier, purchase_order]`.
- **Node chain (7 nodes):** Guard condition (`uufqoeb8xyz`, AND of `status==approved` + `purchase_order==null`):
  - branch 1 (true) → Query default delivery address (`ay8dlnys4ef`) → **Calculation `umk9xiw5aio` (D31, formula.js): `SUBSTITUTE({{$context.data.pr_number}}, "PR-", "PO-")`** → Create purchase_orders (`ubg9mju1tjm`, sets `po_number={{$jobsMapByNodeKey.umk9xiw5aio}}`, `createdById={{$context.user.id}}`) → Create po_lines default line (`4p3q7oq3co5`, writes only `purchase_order`/`description`/`quantity_ordered=1`)
  - branch 0 (false) → Response message (`ylrivonemrq`) → End process (`gviz4zia0ha`, endStatus: -1)
- The `response-message` + `end-process(-1)` pair on the false branch (per [`feedback_inline_guard_end_node`](../../../.claude/projects/-Users-alexander-Documents-Claude-Projects-Havenbeheer-Purchasing/memory/feedback_inline_guard_end_node.md)) is preserved across the D31 revision.
- **Bound to button:** `28jh1q2camo` (Generate PO button, visible on PR table block `l1e2iwdwau9` and PR detail popup `2b367dbd157`). Binding is by key, unaffected by the revision.

### Create-PO Guard (MVP9a)
- **Key:** `vgv8hcrtjvx`
- **Active version ID:** `366562380808192` (enabled=true)
- **Type:** request-interception, global, sync; actions: `create` on `purchase_orders`
- **Node chain:** Query referenced PR (`ww4mxz67ge8`, appends `purchase_order`) → Condition OR pr.status≠approved | pr.purchase_order≠null (`dba34lyg168`) → branch 1: response-message (`7fp12f2018u`) + end (`j57v75y2cky`, endStatus:-1).

### Send PO workflow (MVP9b) — `draft → sent` + budget zones
- **Key:** `send_po`
- **Active version ID:** `367086330314752` (enabled=true, current=true) — **doc-lag corrected 2026-06-07** (was recorded `366981771362304`, which is now a disabled prior version). Node keys/structure unchanged from the description below; the math.js zone-2 fix is carried.
- **Type:** custom-action, sync, collection `purchase_orders`; appends `[purchase_request, supplier]` (supplier appended now for future supplier-email step).
- **15-node chain** (node keys, branch structure):
  - `g_send_guard` (condition, basic) — AND status==draft, total≠null, fx_rate_to_usd≠null.
    - br=0 (false) → `g_send_fail_msg` (response-message) → `g_send_fail_end` (end, -1).
    - br=1 (true) → `calc_po_usd`.
  - `calc_po_usd` (calculation, **math.js**): `{{$context.data.total}} / {{$context.data.fx_rate_to_usd}}` → `c2dibomnrby`.
  - `c2dibomnrby` (calculation, formula.js): `{{$context.data.purchase_request.quoted_total}}/{{$context.data.purchase_request.fx_rate_to_usd}}` (PR total in USD) → `calc_cap_usd`.
  - `calc_cap_usd` (calculation, **math.js**): `{{$jobsMapByNodeKey.c2dibomnrby}} * 1.1` (110% cap) → `z3_check`.
  - `z3_check` (condition, basic `gt`): `calc_po_usd.result > calc_cap_usd.result`.
    - br=1 (true, zone 3) → `z3_msg` (response-message) → `z3_end` (end, -1).
    - br=0 (false) → `z2_check`.
  - `z2_check` (condition, **math.js**): `{{$jobsMapByNodeKey.calc_po_usd}}>{{$jobsMapByNodeKey.c2dibomnrby}}` (zone 2 = PO USD over PR USD).
    - br=1 (true, zone 2) → `oc_check`.
    - br=0 (false, zone 1) → `z1_update` (update status=sent, sent_at=now).
  - `oc_check` (condition, basic) — OR budget_override_comment==null | =="".
    - br=1 (true, comment missing) → `oc_msg` (response-message) → `oc_end` (end, -1).
    - br=0 (false, comment present) → `z2_update` (update status=sent, sent_at=now).
- **Bound to button:** `slybgc23q1i` (Send PO, `RecordTriggerWorkflowActionModel`) on PO surfaces.
- **Engine note:** `z2_check` originally used the basic `gt` calculator and never fired (every PO fell to zone 1); switching the node to math.js fixed it. See auto-memory `feedback_prefer_mathjs_engine`. **Zone 2 verified 2026-05-29** (exec `366982002049024`: PO 2.8 USD vs PR 2.667 USD, cap 2.933 → zone 2, missing comment → rejected). Zone-2 in-app notifications (9b.3) are **not yet built** — gated on Finance `main_approver`.

### Close PO workflow (MVP9d, **post-action event**) — `draft/sent/confirmed/partially_received → closed`
- **Key:** `f8gpu17s6hq` · title "Close PO"
- **Active version ID:** `368791950131200` (enabled=true, current=true) — built 2026-06-07.
- **Type:** **`action` (Post-action event), sync, local mode** (`global:false`), collection `purchase_orders`. Bound by the user to the Close popup EditForm's **Submit** under **Bind workflows → post-action event (local mode)**. (A form Submit can ONLY bind pre-action/post-action/approval — **not** custom-action; the old custom-action close was the wrong type. See auto-memory `feedback_workflow_form_button_pattern` — corrected.)
- **Nodes (2):** `close2_guard` (condition, basic, OR of `equal {{$context.data.status}}` against `draft`/`sent`/`confirmed`/`partially_received`, rejectOnFalse=false) → br=1 (true): `close2_update` (update `purchase_orders` where id=`{{$context.data.id}}`, status=closed, closed_at=now). No false-branch nodes — **a post-action event runs AFTER the update and cannot reject or surface a message** (confirmed via the trigger reference: `type:"action"` = Post-action Event). So this workflow just skips stamping when not closeable; the user-facing rejection is now done by the **Close Guard** request-interception workflow below (D35).
- Flow: the EditForm Submit saves `close_reason` + `close_comment` (its own update), then this post-action workflow stamps `status=closed`, `closed_at`. **Note:** `received` is intentionally NOT closeable (a received PO completes; to bail, correct a line down to revert to `partially_received` first — D33).
- **Deprecated predecessor:** `close_po_draft` (`366780629319680`, custom-action) — **DELETED 2026-06-11** (with user OK, role-hardening session) along with strays `s6m4i5hrmzs` ("Send PO copy"), `jsgbxph9444`, `s4syz7vom4n`.

### Close Guard (D35, 2026-06-09) — reject + message when closing a non-closeable PO
- **Key:** `b6brl8r9c58` · title "Close Guard — block close on non-closeable PO"
- **Active version ID:** `368982201729024` (enabled=true, current=true).
- **Why:** the Close PO workflow is a **post-action** event and can't show a failure message; a Close attempt on a `received` PO previously saved `close_reason`/`close_comment` and silently did nothing (no message). The PO immutability guard `xvcsdv07c5j` only covers `completed`/`closed`, leaving `received` as a silent gap. This guard fills it.
- **Type:** request-interception (Pre-action Event), global, sync; actions: `update` on `purchase_orders`.
- **Nodes (4):** `close_guard_query` (query purchase_orders, multiple=false, filter id=`{{$context.params.filterByTk}}`, failOnEmpty=false) → `close_guard_cond` (condition, basic, **AND**, rejectOnFalse=false) of: `notEqual {{$context.params.values.close_reason}} != null` (a close attempt — same `values.<field>` pattern as the Receive guard, so it does NOT fire on a normal edit that omits `close_reason`) **AND** `{{$jobsMapByNodeKey.close_guard_query.status}}` ≠ each of `draft`/`sent`/`confirmed`/`partially_received` (i.e. status NOT in the closeable set) → br=1 (true=block): `close_guard_msg` (response-message) → `close_guard_end` (end, -1).
- **Logic verified** via `flow-nodes test`: close attempt on `received`/`completed` → block; close attempt on `sent`/`draft` → allowed; normal edit (no `close_reason`) on `received` → allowed (no false-block). **D24 bulk-update limitation applies** (relies on `filterByTk`). For `completed`/`closed` this guard and `xvcsdv07c5j` both fire (redundant, harmless).
- **Caveat:** detection keys on `close_reason` being present in the submitted values. If a future PO **edit** form includes a non-empty `close_reason` field on a `received` PO, it would be blocked — keep `close_reason`/`close_comment` out of the normal edit form (they belong to the Close popup only).

### Complete PO workflow (MVP9d; D34 invoice gate) — `received → completed`
- **Key:** `qh7b3hc5q1r`
- **Active version ID:** `368971625791488` (enabled=true, current=true) — **D34 revision built 2026-06-09: Complete now also requires an attached invoice AND a positive invoice total (USD).** Revision of `368746204954624` (now disabled — see Stale IDs). The predecessor's recorded `executed:0` was stale; `versionStats.executed` was 3 (user's C1 runs completed PO-26-0002/0005), so the change required a same-key revision, not an in-place edit.
- **Type:** custom-action, sync, collection `purchase_orders`. **Trigger appends:** `["invoice"]` (D34 — so the guard can see the attachment; `invoice` is a `belongsToMany` to `attachments`, loads as `[]` when empty).
- **Nodes (8):**
  - `complete_guard` (root; condition, **basic**, AND of `equal {{$context.data.status}} == "received"` **and** `gt {{$context.data.total_usd}} > 0`, rejectOnFalse=false). Basic engine chosen for these two because it returns `false` (not an error) when `total_usd` is null — math.js throws on `null > 0`.
    - br=0 (false) → `complete_fail_msg` (response-message, "Complete is only available for a fully-received PO with an invoice total (USD).") → `complete_fail_end` (end, -1).
    - br=1 (true) → `inv_count`.
  - `inv_count` (calculation, **math.js**): `count({{$context.data.invoice}})` → number of attached invoice files (`[]`→0).
  - `complete_invoice_guard` (condition, **math.js**, `{{$jobsMapByNodeKey.inv_count}} >= 1`, rejectOnFalse=false). math.js used because the basic engine has **no array-emptiness calculator** (`empty`/`notEmpty` are unregistered) and math.js resolves `{{$jobsMapByNodeKey.<key>}}` without `.result`.
    - br=0 (false) → `complete_inv_msg` (response-message, "An invoice attachment is required to complete this PO.") → `complete_inv_end` (end, -1).
    - br=1 (true) → `complete_update` (update `purchase_orders` where id=`{{$context.data.id}}`, status=completed, completed_at=now).
- **Engine split rationale (verified via `flow-nodes test`):** basic has no empty-check + can't count arrays; math.js can't string-compare (`"a"=="b"` → "Cannot convert to number") and errors on `null > 0`. So: string/scalar/null-safe checks (status, total_usd) → basic; array count → math.js. `count()` parses object-array literals fine.
- **Button (BUILT by user — `e60dce0bb2d`):** `RecordTriggerWorkflowActionModel` on the PO detail block `g9xffr68350`, bound to key `qh7b3hc5q1r`. **Linkage rule `us98o7djgw0`** (single `$or`, action=hidden) — hides the button unless completable: hidden when `ctx.record.status $ne "received"` OR `ctx.role $notIncludes "Procurement"` OR `ctx.record.invoice $empty` OR `ctx.record.total_usd $empty` (D34 added the last two; block renders `invoice`/`total_usd` so `ctx.record` carries them). The guard is the hard stop; the button just mirrors it. **Note:** linkage `$empty` won't catch `total_usd == 0` (a value); the server guard's `> 0` does.
- **Verification:** node logic validated via `flow-nodes test`; **end-to-end C1 re-verification PENDING** (user drives the Complete button — custom-action can't be `workflows execute`'d without a user, per `feedback_custom_action_execute_no_user`).

### Guard: PO Immutability (MVP9d) — lock terminal PO header
- **Key:** `xvcsdv07c5j`
- **Active version ID:** `368747670863872` (enabled=true, current=true) — built 2026-06-07, verification pending.
- **Type:** request-interception, global, sync; actions: `update` + `destroy` on `purchase_orders`. Mirrors PR Guard A.
- **Nodes (4):** `po_imm_query` (query purchase_orders, multiple=false, filter id=`{{$context.params.filterByTk}}`, failOnEmpty=false) → `po_imm_cond` (condition, basic, OR `equal {{$jobsMapByNodeKey.po_imm_query.status}}` against `completed`/`closed`, rejectOnFalse=false) → br=1: `po_imm_msg` (response-message, "This PO is finalized and can no longer be edited.") → `po_imm_end` (end, -1).
- **D24 bulk-update limitation applies** (relies on `filterByTk`; bulk update via `filter.$and[0].id.$in` bypasses it) — documented, not fixed (C5). **Payment caveat:** see D33.

### Guard: PO Line Immutability (MVP9d) — lock lines of a terminal PO
- **Key:** `f3dkb37te22`
- **Active version ID:** `368747750555648` (enabled=true, current=true) — built 2026-06-07, verification pending.
- **Type:** request-interception, global, sync; actions: `update` + `destroy` on `po_lines`. Separate from the 9c Receive Guard (`mhfp4d15uee`, which only blocks `received_quantity` on non-receivable POs).
- **Nodes (4):** `line_imm_query` (query po_lines, multiple=false, filter id=`{{$context.params.filterByTk}}`, **appends `[purchase_order]`**, failOnEmpty=false) → `line_imm_cond` (condition, basic, OR `equal {{$jobsMapByNodeKey.line_imm_query.purchase_order.status}}` against `completed`/`closed`, rejectOnFalse=false) → br=1: `line_imm_msg` (response-message, "Lines of a finalized PO can no longer be edited.") → `line_imm_end` (end, -1).
- Workflow-internal updates (9c receiving recompute) bypass request-interception (`feedback_request_interception_scope`), so receiving is unaffected.

### Guard: PO Line Create (D45, 2026-06-13) — block adding a line to a terminal PO
- **Key:** `polncreateg1` · title "Guard: PO Line Create — block on terminal PO"
- **Active version ID:** `369880930516992` (enabled=true, current=true).
- **Why:** the Line Immutability guard `f3dkb37te22` covers only `update`/`destroy`, so a **new** line could still be created on a `completed`/`closed` PO. This closes the create gap.
- **Type:** request-interception (Pre-action Event), global, sync; actions: `create` on `po_lines`.
- **Nodes (4):** `plc_query` (query purchase_orders, multiple=false, filter id=`{{$context.params.values.purchase_order}}`, failOnEmpty=false) → `plc_cond` (condition, basic, OR `equal {{$jobsMapByNodeKey.plc_query.status}}` against `completed`/`closed`, rejectOnFalse=false) → br=1: `plc_msg` (response-message "Cannot add a line to a finalized (completed or closed) PO.") → `plc_end` (end, -1).
- **Route coverage (D45):** the guard reads the parent FK from `{{$context.params.values.purchase_order}}`. Both create routes now carry it:
  - **Direct** `po_lines:create` — FK is in the body natively.
  - **Association** `purchase_orders.lines:create` (the add-line form) — the FK is **not** in the body by default (request-interception fires but `values.purchase_order`/`purchaseOrderId`, `filterByTk`, `associatedIndex`, `sourceId` are all empty; FK injected after the guard). **Fixed (user, 2026-06-13):** the add-line form's Submit button **`8373f6c1297`** (parent form `fd22f7bcaed`) has `assignFieldValues.purchase_order = {{ ctx.popup.record.id }}`, which injects the parent PO id into the submitted values so the guard can query it. So the association route is now guarded server-side (form-hide on terminal POs is optional UX polish, not the control).
  - The query fails open (status null → allow) if no FK is present, so legit draft-PO line-adding is never false-blocked. See auto-memory `feedback_request_interception_assoc_create_no_source`.
- **Verified 2026-06-13 (fully):** `flow-nodes test` (completed/closed→block, draft/empty→allow); live direct-create probe (completed→block, draft→line created); live **association**-create replay with `purchase_order` in values → completed PO **blocked**; **user confirmed via the real add-line form** — blocks on a terminal PO, allows on a draft PO (`{{ ctx.popup.record.id }}` resolves at submit). D24 bulk-create caveat: a bulk/`filter.$in` create isn't intercepted.

### Budget guards + line recompute (D47, 2026-06-14) — per-line budget hard-block in PR currency
These **stack** on the existing terminal guards (`polncreateg1` create, `f3dkb37te22` update/destroy) — both fire on the same `po_lines` action; first to `end(-1)` wins (verified live). Ceiling = `PR.quoted_total` in the PR's own currency (PO currency is PR-locked → same-currency numeric compare, no USD/FX).

- **Budget create guard** — key `8u81nd3vxhc`, id `370018742763520`, request-interception `po_lines:create`, global sync. Nodes: `cond_haspx` (basic, AND: `values.unit_price != null`, `values.quantity_ordered != null`) → br=1: `q_po` (query purchase_orders by `{{$context.params.values.purchase_order}}`, appends `[purchase_request]`) → `sum_lines` (aggregate sum `line_total` where `purchase_order.id == values.purchase_order`) → `calc_new` (formula.js `N({{sum_lines}}) + {{values.quantity_ordered}} * {{values.unit_price}}` — `N()` coalesces empty-set null→0, the first-line case) → `cond_over` (math.js `{{calc_new}} > {{q_po.purchase_request.quoted_total}}`) → br=1: response-message (dynamic, echoes amount+`quoted_currency`) → end(-1). When no price/qty → falls through → allow.
- **Budget update guard** — key `c9c14tyn876`, id `370019308994560`, request-interception `po_lines:update`, global sync. Nodes: `cond_compedit` (basic, OR: `values.quantity_ordered != null`, `values.unit_price != null` — **skips received_quantity-only writes**, so receiving passes) → br=1: `q_line` (query po_lines by `filterByTk`, appends `[purchase_order, purchase_order.purchase_request]`) → `sum_excl` (aggregate sum `line_total` where `purchase_order.id == q_line.purchase_order.id` AND `id != filterByTk` — **excludes self**) → `eff_qty`/`eff_price` (formula.js `IF(ISBLANK({{values.x}}), {{q_line.x}}, {{values.x}})` — coalesce partial updates to stored) → `new_total` (formula.js `N({{sum_excl}}) + {{eff_qty}} * {{eff_price}}`) → `cond_over` (math.js `> q_line.purchase_order.purchase_request.quoted_total`) → br=1: response-message → end(-1).
- **Recompute A** — key `5ukanitoy74`, id `370018017148928`, collection sync, `{collection:po_lines, mode:3, changed:[quantity_ordered,unit_price]}` (create + price/qty update). **Recompute B** — key `pnvp0dtitum`, id `370018323333120`, collection sync, `{collection:po_lines, mode:4}` (delete). Two workflows because collection-trigger `mode` only accepts `[1,2,4,3]`. Both: `q_po` (query PO by `{{$context.data.purchaseOrderId}}` — scalar, delete-safe) → `sum_lines` (aggregate sum `line_total` for the PO) → update `purchase_orders.lines_total` → `cond_issued` (basic `status != draft`) → br=1: update `needs_reprint=true`.

### Issue PO workflow (D46; **all-priced gate + needs_reprint added D47**)
- **Key:** `issue_po` · custom-action, sync, collection `purchase_orders`, trigger appends `[lines]`.
- **Active version ID (D47):** **`370019772465152`** (enabled=true, current=true). Prior `369914017284096` retired (disabled, non-current) — **stale, do not use**.
- **Nodes:** `issue_guard` (basic: `status==draft` + supplierId/deliveryAddressId/currency not-null + `total>0`) →br1 `issue_count` (math.js `count(lines)`) → `issue_lines_guard` (`count>=1`) →br1 **`uxpi9ufle06`** (aggregate count `unit_price>0`) → **`x01errm96yk`** (math.js `count_priced == issue_count`) →br1 `issue_update` (`status=issued`, `issued_at=now`, **`needs_reprint=false`**); →br0 response-message "every line needs a unit price greater than 0" → end(-1). Each guard's br0 = reject+end(-1).
- **NOT CLI-verifiable** (custom-action has no user when executed headless); structure verified, **awaiting user UI walkthrough**.

### Receive Guard (MVP9c) — block receiving against a non-receivable PO
- **Key:** `mhfp4d15uee`
- **Active version ID:** `368072131870720` (enabled=true, current=true) — built + verified 2026-06-07 (R4 passed).
- **Type:** request-interception, global, sync; actions: `update` on `po_lines`.
- **Node chain (4 nodes):** Query line+parent PO (`567k7v8jzsi`, filter id=`{{$context.params.filterByTk}}`, appends `[purchase_order]`) → Condition (`74kymxgok4k`, basic, **AND** of 5 `notEqual` leaves: `received_quantity` present (`{{$context.params.values.received_quantity}} != null`) AND PO.status ≠ each of sent/confirmed/partially_received/received) → br=1 (true=block): response-message (`9c4j0rzf24t`) → end (`03fvl5yb4vy`, endStatus −1).
- **Design note:** blocks only when the submitted values carry a non-null `received_quantity` AND the PO isn't receivable — so editing a `draft` PO's lines stays allowed *provided `received_quantity` is empty on that form*. Bulk-update caveat (D24) applies. Workflow-internal update nodes bypass this guard (`feedback_request_interception_scope`).

### PO Receiving recompute workflow (MVP9c) — derive line_status + PO header
- **Key:** `ork27v016yo`
- **Active version ID:** `368710576439296` (enabled=true, current=true) — **doc-lag corrected 2026-06-07** (was recorded `368072534523904`; a later user revision is now active). R1–R3 verified on the lineage.
- **Type:** collection event, **sync**, collection `po_lines`; **mode=2 (update), `changed:["received_quantity"]`** (loop guard — the workflow's own line_status/header writes don't re-fire), appends `[purchase_order]`.
- **Node chain (10 nodes):**
  - `nys8gwon5ic` (calculation, **formula.js**) → `line_status` = `IFS(AND(quantity_ordered>0, received_quantity>=quantity_ordered),"received", received_quantity>0,"partially_received", true,"pending")` (reads `{{$context.data.*}}`).
  - `dbaahktlgww` (update po_lines, batch/`individualHooks:false`) → write `line_status={{$jobsMapByNodeKey.nys8gwon5ic}}` to the trigger line (`id={{$context.data.id}}`).
  - `b8bd5brza19` (aggregate count, **A**) → po_lines where `purchase_order.id=<PO>` AND `line_status != received`.
  - `jsj8bqoihag` (aggregate count, **B**) → po_lines where `purchase_order.id=<PO>` AND `received_quantity > 0`.
  - `y7lkvu5cxkx` (condition, **math.js**, rejectOnFalse=false) `{{$jobsMapByNodeKey.b8bd5brza19}} == 0` (all received?):
    - br=1 (true): `913yhkkni8r` Update PO `status=received` → `9wr1evj9pjj` Query Procurement dept (id `363554444476416`, appends `[main_approver]`) → `7421bepct6m` Notification (channel `approval-todo-in-app-message`, receivers `["{{$jobsMapByNodeKey.9wr1evj9pjj.main_approver.id}}"]`=Pat, `ignoreFail:true`, "PO {{po_number}} is fully received and ready to complete.").
    - br=0 (false): `7sncivamuep` (condition, **math.js**) `{{$jobsMapByNodeKey.jsj8bqoihag}} > 0` (any receipts?):
      - br=1 (true): `9tot1o6z03u` Update PO `status=partially_received` (covers R1 + R3 reverse).
      - br=0 (false): no node — header unchanged.
- **Verification status:** line_status formula + both math.js conditions validated via `nb api workflow flow-nodes test`; full R1–R4 passed end-to-end via the user-built receiving UI 2026-06-07.

---

## Test users

| Username | ID | Main dept | Roles (besides member) |
|---|---|---|---|
| alice.member | 9 | Operations (363554454962178) | operations (via dept) |
| oliver.owner | 10 | Operations (363554454962178) | operations (via dept) |
| pat.procurement | 11 | Procurement (363554444476416) | procurement |
| dana.director | 12 | Director (363554454962176) | director |
| simon.supervisor | 13 | Operations (363554454962178) | operations (via dept; not shown in users.roles append — dept-inherited at runtime) |

| fiona.finance | 14 | Finance (363554454962177) | finance (via dept; not in users.roles append — dept-inherited at runtime). Created Phase 2 (D40), password `Test1234!` |

---

## Roles & ACL (hardened 2026-06-11, phases 0–1 — D38)

- **roleMode:** `allow-use-union` (each session still runs on ONE selected role; union is an option in the user-menu role switcher).
- **member** (base, default): snippets `["!pm","!pm.*","!ui.*"]` (vanilla, `ui.*` workaround reverted), strategy `{"actions":["view"]}` — view-only everywhere.
- **director**: strategy `{"actions":["view"]}` + independent `purchase_requests` row: view (all 37 fields) + **render-enabler** update (2 fields: `rejection_reason_category`,`rejection_comment`; scopeId 10 "PR — director stage"). Needed so the approval ProcessForm renders (see CLAUDE.md rule / auto-memory).
- **operations** (NEW, bound to Operations dept `363554454962178` via `departments.roles`): no strategy; independent rows: `purchase_requests` view(37) / create(19) / update(19, scopeId 2 "PR — own and editable"); `attachments` view+create. Desktop routes: same 11 as member. Create/update field list = the 18 submitter fields + `quoted_total_usd` (display in forms; server recomputes).
- **procurement**: strategy `view/trigger/update` (unchanged); independent rows rewritten: `purchase_requests` view(37)/update(22, excludes status+stamps)/trigger/triggerNew/export(37) — **create + importXlsx REMOVED (D25 now enforced)**; `purchase_orders` view(31)/create(16)/update(16, excludes status/po_number/stamps/total_usd/purchase_request-relink)/destroy/trigger; `po_lines` view(12)/create(5+id, user-added)/update(5, excludes line_status)/destroy; `attachments` + `suppliers` rows untouched (pre-existing, working — note their scopeId `363334209503233` is the built-in "All records" scope). **Also has independent rows (undocumented until 2026-06-12) for `units_of_measure`, `products`, `delivery_addresses`** — full view/create/update/destroy with field whitelists, scope "All records".
- **Nested-picker `id` whitelist fix (2026-06-12, verified by user):** Pat's PO add-line form (association create `purchase_orders.lines:create?updateAssociationValues[]=unit_of_measure` on the Line Items tab, CreateFormModel `fd22f7bcaed`) failed with "Name already exists" — the server ACL middleware `checkChangesWithAssociation` pruned the nested UoM object to procurement's `units_of_measure` **update** field whitelist, stripping `id` → the link became a duplicate-create against the unique `name`. Fixed by adding `id` to the `units_of_measure` **update** field list (adding it to `create` alone does NOT fix it — the update branch is taken when the picked record exists; root bypasses the middleware entirely, which is why admin worked). **Trap disarmed for the other lookups too (same day):** `id` added to the **update** whitelists of `delivery_addresses` (actionId `366562898804749`) and `products` (actionId `366562898804759`); `suppliers` already had `id` (full-field whitelist). See auto-memory `feedback_acl_nested_association_id_whitelist`.
- **finance** (**Phase 2 / D40, 2026-06-11**): `null` strategy; independent `purchase_requests` row: view (all 37 fields) + **render-enabler** update (2 fields: `rejection_reason_category`,`rejection_comment`; scopeId 10) — mirror of director's row. Desktop routes: same 11 as member (was zero). User: fiona.finance (14, via Finance dept binding). No finance approval stage exists yet — groundwork for the payment MVP.
- **Scopes** (`rolesResourcesScopes`): only **4 remain** — 2 "PR — own and editable" (**fixed 2026-06-11**: `submitter_id`→`submitterId`; filter: status ∈ {draft,info_requested} AND (createdById|submitterId = $user.id)); 4 "PR — pending purchasing review"; 5 "PO — non-terminal"; 10 "PR — director stage" (status ∈ {pending_director_approval,pending_board_approval}). **Scopes 3, 6–9 DELETED 2026-06-13 (D43)** — were inert snake_case-broken dead data; their 8 orphan action rows deleted too. Backup `backups/acl-scopes-3-6-9-DELETED-20260613.json`.
- **Workflow-managed columns** (`status`, `approved_at`, `cancelled_at`, PO stamps, `po_number`, `line_status`) are now enforced read-only via ACL field whitelists per user preference (D38) — no longer reliant on form `readPretty` only. Strategy-view roles can read but not write them.
- **D41 note (2026-06-12):** dropping fields auto-scrubbed them from ACL field whitelists — the "view (all 37 fields)" lists above are now 35 for `purchase_requests` (platform-maintained; no manual re-apply was needed).

---

## UI

- **Purchase Requests page UID:** `cuycec133qb`
- **Table block:** `l1e2iwdwau9` — columns include title, status, quoted_total, quoted_currency, `expenditure_type`, `is_emergency`, `needed_by` (MVP8), `pr_number` (D31 — column wrapper `05rzpmr4pxk`, DisplayTextFieldModel)
- **PR view popup** (DetailsBlockModel `2b367dbd157`): shows all PR fields incl. quote fields, `supplier`, and the four MVP8 fields. The popup grid `5fb7b74fa30` contained an MVP8 Comments block `52t8wtbzni4` bound to `purchase_requests.comments`; the user removed it post-verification. Re-add via `nb api flow-surfaces add-block` with type `comments` and resource `{binding:"associatedRecords", associationField:"comments"}` targeting the grid.
- **Procurement approval form** (now per-revision, detached from template `k60b738pjy0`): current ProcessFormModel uid varies per workflow revision. MVP8 read-only fields applied. **D37/MVP013:** on the active revision `369161752084480` the ProcessForm grid is `03uy1easu6l` — gained **editable** `is_regular` (CheckboxFieldModel, wrapper `dcbp2xasi5f`, inner `w5v1zvd8t00`, tooltip on the regular-purchase rule). (Procurement sets `is_regular` here; the condition reads it.)
- **PR create form** (CreateFormModel `e76c40c8c79`, template `n9f8v5vnhhc`): originally included a `needs_director_approval` checkbox after justification (linkage rule made justification required when checked) — field dropped 2026-06-12 (D41), form was already clean. MVP8 added `expenditure_type`, `needed_by`, `is_emergency`, `other_attachments` after `needs_director_approval`. **MVP010** added `skip_dept_approval` (CheckboxFieldModel, wrapper `830iodzmcjo`) — **GONE: user rebuilt the create form and dropped both `skip_dept_approval` and `needs_director_approval` from it** (discovered MVP012; the recorded wrapper IDs are stale). **D36/MVP012** added `use_custom_approver` checkbox (wrapper `r9iu06my5ot`) + `custom_approver` picker (wrapper `gfozpat06nw`, inner `RecordPickerFieldModel mu4s7d3w0h2`, selector `TableSelectModel ueeb9ybhmuc`) to grid `5c325101ecc`, with field-linkage rules (fingerprint-based) that show+require `custom_approver` when `use_custom_approver` is on and hide it otherwise. **Picker data scope (same-dept + exclude-self) is NOT set via CLI** — public authoring exposes no data scope on the record picker; set it via the form's "Set data scope" UI setting (manual). Self-approval is independently blocked in the workflow condition.
- **PR detail popup** (`2b367dbd157`): MVP010 added `skip_dept_approval` read-only (DisplayCheckboxFieldModel, wrapper `in24ndj91et`) — **removed in MVP012**. **D36/MVP012** added `use_custom_approver` (wrapper `7jjizc66ex2`) + `custom_approver` (wrapper `9tpfw54yy1e`) read-only. **D31** added `pr_number` read-only (DisplayTextFieldModel, wrapper `24fsysz731w`). **D32** added `board_approval_document` read-only on grid `16975baef39` (DisplayPreviewFieldModel, wrapper `ive719bxqm2`). **D37/MVP013** added `is_regular` read-only on grid `16975baef39` (DisplayCheckboxFieldModel, wrapper `6q8zzia1bt7`, inner `n6l48asyh9p`). The retired `needs_director_approval` read-only display (wrapper `p9acb4e35of`) was **removed 2026-06-12 (D41)** along with the field.
- **Generate PO button** (`28jh1q2camo`, MVP9a): `RecordTriggerWorkflowActionModel` on PR table row popup and PR detail popup. Bound to workflow key `2izsx8uv50r`. Two linkage rules:
  - Hide when `record.status != "approved"` OR `record.purchase_order is not empty`.
  - Hide when `ctx.user.roles.title` does not include `"Procurement"` (procurement-only visibility; see [feedback_linkage_rules_user_roles](../../../.claude/projects/-Users-alexander-Documents-Claude-Projects-Havenbeheer-Purchasing/memory/feedback_linkage_rules_user_roles.md) in auto-memory for the pattern).
- **Duplicate button** (user-added 2026-06-25, D51): a **Duplicate** record action on the PR detail popup `2b367dbd157`, using the **"Duplicate to form"** mode (`@nocobase/plugin-action-duplicate`, ships built-in). Clicking it opens a pre-filled PR **create form** seeded from the source PR; the user edits and submits to create a brand-new PR (fresh `pr_number`, `status` back to default). Purpose: repeat a recurring purchase, or re-apply a **rejected** PR after corrections. **Visibility linkage:** shown only when `record.status` is `approved` **or** `rejected` (i.e. a PR in a final state) — hidden in all in-flight states. Duplicate semantics (per the [NocoBase docs](https://docs.nocobase.com/interface-builder/actions/types/duplicate)): the action auto-excludes primary/foreign keys, unique fields (`pr_number`), sequence fields, and timestamp/user-tracking fields; m2o picker fields (`supplier`, `department`, `custom_approver`, `project`) are copied as **references** (link to the same target records), and since the PR collection has no sub-form/sub-table fields nothing is deep-copied. The exact template-field selection was configured by the user in the UI. **Button uid not captured** (no read/describe path for nested flowModels actions via CLI; `use`/`parentId` aren't filterable) — record it the next time this popup is edited. The `purchase_orders` detail block has no equivalent (POs are workflow-generated from PRs, not hand-duplicated).

Approval form surface IDs on the active version: see "Approval surfaces" above.

### Theme (theme-editor plugin, set live 2026-06-24)
- **Active theme:** `themeConfig` record `id 6`, uid `h5le3tm19gc`, name **"Havenbeheer"**, `default: true`, `isBuiltIn: false`. Built-in Default (`id 1`) left untouched. Edit via `nb api resource update --resource themeConfig --filter-by-tk 6 --values '{"config":{...}}'` (the dedicated `nb api theme-editor theme-config update --id` does NOT map id→filterByTk — it errors "filter or filterByTk is required"; use the generic `resource update` instead). Read all themes: `nb api theme-editor theme-config list`.
- **Tokens (AntD seeds, derived from Havenbeheer Suriname brand — vibrant grass green + deep forest green on white):**
  - `colorPrimary` `#1F7A43` (legible "haven green", ≈4.6:1 on white — chosen over the brand vibrant `#43A82F` which fails white-text contrast, and over the old `#004d39` which read near-black)
  - `colorPrimaryBg` `#E3F0E8`
  - `colorSuccess` `#43A82F` (brand grass green), `colorWarning` `#D89A0C`, `colorError` `#C0392B`, `colorInfo` `#2B7BB9` (kept blue so "info" ≠ a status green)
  - `colorTextBase` `#1D2A23`; sizing `fontSize 15`, `controlHeight 36`, `borderRadius 6`; `motionUnit 0.03`
  - Header/sider chrome: `colorBgHeader` `#16442E`, `colorPrimaryHeader` `#001529`, header-menu text/hover tints `#ffffffa6`/`#ffffff`, `colorBgSiderMenuActive` `#cbdcd5`
  - Config-mode accent (left at NocoBase default): `colorSettings` `#F18B62` + its hover tints
- **NOTE — status tags don't read theme tokens.** The Approved/Pending/Rejected tags in the PR status column come from per-option `color` values on the `purchase_requests.status` select field (AntD preset tag palette: green/gold/red/etc.), NOT from `colorSuccess`/`colorWarning`/`colorError`. To brand them, edit each option's color in the field config — not the theme.

### Purchase Orders page (MVP9a/9b)
- **Page UID:** `liwmklclbnc`
- **Table block:** `vldbcvf41r6`
- **PO detail popup (DetailsBlockModel):** `g9xffr68350`; row-popup template `wp2s32qgfeg`.
- **Send PO button** (`slybgc23q1i`, MVP9b): `RecordTriggerWorkflowActionModel`, bound to workflow key `send_po` (set via `nb api resource update --resource flowModels`). Linkage: hide when `record.status != "draft"`; procurement-only (`ctx.user.roles.title` not-includes `"Procurement"`).
- **Print button** (`c579329db0d`, MVP9e): `TemplatePrintRecordActionModel` on PO detail block `g9xffr68350`, beside Send. Bound to template `kkooshlz8rf` (`props.templateName`), `convertedToPDF:true`. Renders the branded PO PDF via Carbone+LibreOffice (server-side). No linkage rules (visible to all PO viewers). See the `printingTemplates` collection + MVP9e notes below.
- **Close PO button** (`lylrxwl1b3g`, MVP9b): `PopupCollectionActionModel` — opens a popup with an EditForm bound to the current record. Form fields: `close_reason` (`qou6ge8axe5`, required) + `close_comment` (`h5872h3mc8r`, required). Submit action `5ove3dxktz9` triggers workflow key `close_po_draft`. Same draft + procurement-only linkage as Send. (Close-from-non-draft broadens this in 9d.)
- **Add-line form** (Line Items tab, CreateFormModel `fd22f7bcaed`, association create `purchase_orders.lines:create`): Submit button **`8373f6c1297`** carries `assignFieldValues.purchase_order = {{ ctx.popup.record.id }}` (D45) — injects the parent PO id into the submitted values so the **PO Line Create guard** `polncreateg1` can query the parent and block adds to a finalized PO. Do not remove this assign — it's the only thing that makes the association create guardable.
- **Line-items total block (JS Block / RunJS, built 2026-06-24, user-placed):** a JavaScript block at the bottom of the PO popup's **Line Items tab** showing **Σ(`po_lines.line_total`) + PO currency** ("Total of all line items — 12.345,67 SRD"). Reads the parent PO via `ctx.getVar('ctx.record')`/`ctx.popup.record`; sums via a reactive `MultiRecordResource` on `po_lines` filtered `{purchase_order:{id:poId}}`. **Refresh** wired by the user on the add/edit-line form Submits (*After successful submission → Refresh data blocks* → this block) — the resource `'refresh'` handler recomputes. Verified working incl. manual refresh. **Canonical code: [snippets/po-line-items-total.js](snippets/po-line-items-total.js)** (RunJS is not version-controlled, so the repo holds the source of truth). Block uid not captured — the doc's Line Items grid uid `1d42bf8539c` is **stale** (it holds only the add-form + table `f3e868e9267`; the live tab grid has drifted, consistent with the noted PO-surface rebuild). No native table summary footer exists in v2 — hence the JS block (see auto-memory `feedback_table_no_native_summary_footer`).

### Suppliers page (MVP7)
- **Page UID:** `gbg516xq3ie` (desktopRoute `366222348582912`, flowPage); tab "Suppliers" schema `pay5ldoqznb` (route `366222348582913`).
- Block-level uids (table/detail/forms) not captured yet — record them the next time this page is edited.

---

## Stale IDs (DO NOT USE)

### Workflow versions of `cv237r8h7k9` (all disabled before the active `369536223739904`):
- `369495666917376` — the D40 data-driven-approvers revision (2026-06-11), superseded by the user's 2026-06-12 revision `369536223739904` (slim board form + Reject action). Its surfaces (trigger `a88dhcc6p8k`/`yuli5bfnwfu`, Procurement `fjrw5u3cxxg`/`n7bjt8nz4uy`, Dept `h84ic5epwko`/`mjy73tr4cid`, Director `7qnayjp9lzl`/`k7ynav1jxw4`, Custom `wc4et0bt8si`/`qld8db015tw`, Board `r3w4l3ibkui`/`z1o8zfquxgn`) are stale. Disabled. (A short-lived agent revision `369538100690944` cut from this version on 2026-06-12 was deleted the same session — superseded before enabling by the user's revision.)
- `369166026080256` — user revision (hardcoded Director [12] / Board [11] assignees), superseded by the D40 data-driven revision `369495666917376` (2026-06-11). Disabled.
- `369161752084480` — the D37/MVP013 `is_regular` revision, superseded by a user revision `369166026080256` (found live 2026-06-11). Disabled.
- `369154168193024` — a **user revision** that was active immediately before MVP013 (the doc had wrongly listed `369076269481984` as active); 30 nodes. Superseded by the D37 `is_regular` revision `369161752084480`. Disabled.
- `369076269481984` — the D36/MVP012 custom-approver revision (doc-recorded active, but actually superseded by the user revision above). Disabled.
- `368983543906304` — active immediately before MVP012 (2026-06-09); 27 nodes (still had the skip branch). Superseded by the D36 custom-approver revision `369076269481984`. Disabled.
- `368641179582464` — previously (wrongly) recorded as active; superseded. Disabled.
- `367885604880384` — was the D32 board-approval active version (2026-06-02 → sometime before 2026-06-07); superseded by a later user revision `368641179582464` (current). Its approval-surface IDs (recorded in the PR Approval section above) may now be stale; not re-inspected this session.
- `367158084370432` (was active D30 $300-floor, replaced by D32 board-approval revision 2026-06-02). Its approval surfaces are now stale: trigger approvalUid `oudb91ahp0g`/taskCardUid `84gqev1gycl`; dept `x1v8vfcjrnv`/`g93one3xwn9`; procurement `1zfnz7s6in2`/`zbx9zt781mg`; director `04fmmdcx1p9`/`j5uzaf7vnnn`.
- `367150157135872` (was active MVP010, replaced by D30 $300-floor revision 2026-05-30). Its approval surfaces are now stale: trigger approvalUid `eau2jcelpdt`/taskCardUid `samikialtou`; dept `qvig0h56ixs`/`2570ru6tzn4`; procurement `bix5r31hbtr`/`82brq5d17mn`; director `44zwoatqddy`/`ftbqbeatyo5`.
- `366549533655040` (was active MVP8→MVP9b, replaced by MVP010 revision 2026-05-29). Its approval surfaces are now stale: trigger approvalUid `1yw73plyqsf`/taskCardUid `e6edajqk51d`; dept `0qljvpsiceo`/`pdbm4aixrc9`; procurement `z01rza37pod`/`bvlz1vbvi7t`; director `6x42w7n9h4g`/`aahsde3cnie`.
- `366523411529728` (MVP8 intermediate revision, superseded by user's follow-up edits → `366549533655040`)
- `366523398946816` (MVP8 duplicate revision created accidentally, never enabled — destroying it needs explicit user OK per CLAUDE.md irreversible-action rule)
- `366234405109760` (was active up to MVP8, replaced 2026-05-26)
- `366232953880576`
- `366207890817024`
- `366087730298880`
- `366086440550400`
- `366059727028224`
- `366057076228096`
- `366053490098176`
- `365711034417152`
- `365001941123072`

### Approval surfaces from disabled workflow versions:
- From version `366234405109760` (prior active): trigger approvalUid `no4q0qifkv2` / taskCardUid `fc8790fw6pd`; dept `0x4yjm74y0o` / `jmy6o8nkdld`; procurement `zbvpqgod2bs` / `39ynx9u1zlh`; director `nnzr393hos1` / `wet1jqjv8t2`.
- From version `366523411529728` (MVP8 intermediate): trigger approvalUid `svezxiek2gk` / taskCardUid `ndpn7l9cnif`; dept `wankth4i85p` / `iwqxqf5j5p6`; procurement `4qbsr41frsw` / `jwwayk35jmg`; director `mcmyxnng8q7` / `pa83sj9uoke`.
- From version `366232953880576`: approvalUids `2zmok19gb2c`, `7xwj8l0sjqp`, `knwxauc0yoz`, `lav2su037qi`; taskCardUids `exgm0gh0mru`, `5l5vdolh5su`, `ivg75pqfe6b`, `arpce782zod`.
- Older disabled-version surfaces: `5sewfvayoc4`, `ylccjkdatwa`, `wa1guuahjjo`, `4ceoua2g0ij`, `klak6hh6vu0`, `qswcu5p6ihj`, `42ay2w0j69v`, `apz6gdy0z6z`, `n7n6x0xg3t0`, `wdty2zx7de7`, `8yyu6ofo1ww`, `rgcyt60s8pg`, `yyptfj0azru`, `o4jc2ghrs4q`, `8x5ktd74gwx`, `o1n99mp7sn7`

### Stale workflow keys:
- `p4n6dffjcgq` / version `364960795000832` — does not exist
- `p1tnx6nb5r9` / version `364995697901568` — disabled rebuild from between sessions
- `idezsq1k1ts` / `363982109736960` — original v3-plan MVP1 key, superseded
- `1r4vyfbnie8` — hardcoded on the Generate-PO button before workflow build; no workflow with this key ever existed.

### Stale Generate-PO (`2izsx8uv50r`) versions (all disabled before the active `368786562547712`):
- `367255610327040` — the D31 revision (7 nodes incl. default-line create); superseded by the user's 6-node revision `368786562547712` (default-line create removed on purpose). Disabled.
- `366569458696192` — initial build with JSON-filter embedded guard.
- `366595041853440` — round-2 revision that wrongly removed the inline guard.
- `366602797121536` — interim revision.
- `366608098721792` — round-3 revision: restored guard but missed the end-process node on the false branch.
- `366623370182656` — interim user revision.
- `366623590383616` — was recorded as active through the $300-floor session; superseded by user revisions.
- `366626266349568`, `366626572533760` — later user revisions; `366626572533760` was the live active version (executed=9) immediately before the D31 revision.
- All superseded 2026-05-30 by `367255610327040` (current, D31).

### Stale Send-PO (`send_po`) versions (all disabled before `367086330314752`):
- `366981771362304` — was recorded as active through MVP9c; superseded by a later user revision `367086330314752` (current). Earlier disabled versions below.
- `366776493735936`, `366882024521728`, `366883251355648` — early build iterations.
- `366883769352192` — had the zone-2 bug (basic-engine `gt` condition that never fired).
- `366980364173312` — a revision where the agent patched the zone-2 condition with `.result` (wrong theory); superseded by the user's math.js fix in `366981771362304`.
- `366980536139776` (**key `s6m4i5hrmzs`**, title "Send PO copy") — stray duplicate; **DELETED 2026-06-11** (user OK).

### Stale Complete-PO (`qh7b3hc5q1r`) versions (disabled before `368971625791488`):
- `368746204954624` — original MVP9d build (status==received only, no invoice gate). Executed 3× (user C1 runs). Disabled 2026-06-09 by the D34 invoice-gate revision `368971625791488` (current).

### Disabled po_lines collection workflows (cancelled per D27):
- `jsgbxph9444` ("PO Total: Lines Added/Updated") and `s4syz7vom4n` ("PO Total: Line Deleted") — **DELETED 2026-06-11** (user OK, role-hardening session).

---

## Notes for the next session

- **D47 (PO line pricing + per-line budget block) — CORE + UI BUILT 2026-06-14; one user step left.** Data model + 4 new workflows + `issue_po` revision + UI all live & enabled (see D47 in decisions.md + workflow sections). **Stale: old `issue_po` version `369914017284096`** (retired; current = `370019772465152`). **REPRINT FEATURE FULLY STRIPPED (2026-06-14, user request — it kept mis-refreshing).** Removed entirely: banner block `85y72gu69iz` + "Mark reprinted" button `frx88dkutxu` (UI); clear-flag workflow `t7vu4n7dhhx` (**destroyed**); the `needs_reprint` field (**dropped** — ACL whitelists auto-scrubbed). The two recompute workflows were **revised** to drop their condition+flag nodes — now `lines_total`-only: `5ukanitoy74` v`370047322750976` (create/update) and `pnvp0dtitum` v`370047503106048` (delete). `issue_po` was **revised** to v`370047775735808` to drop its `needs_reprint=false` write (all-priced check + issue logic retained). Swept all current workflows: **no node references `needs_reprint`**. The PO-line **budget guards remain active** (`8u81nd3vxhc` create-ceiling, `c9c14tyn876` update-ceiling, plus terminal guards `polncreateg1`/`f3dkb37te22`). Print template trimmed to a single **Order Total** (from `lines_total`); Invoice Total / Invoice Total (USD) rows removed; line-table columns right-aligned except Description. _Removed UI (gone):_ former "Mark reprinted" button **`frx88dkutxu`** (RecordTriggerWorkflowActionModel bound to custom-action workflow **`t7vu4n7dhhx`** "PO: clear reprint flag" → update `needs_reprint=false`; `afterSuccess.actionAfterSuccess=stay` + a `refreshTargetBlocks` eventflow on `g9xffr68350`, mirroring the Issue button so the detail block re-fetches and the banner re-evaluates) + actionLinkage hiding it unless `record.needs_reprint $isTruly`; markdown indicator **`85y72gu69iz`** in details grid `2kmv5ps2xef` (blockLinkage hidden unless needs_reprint truthy). **NOTE:** the first attempt used an `updateRecord` button (`skw701v130z`, removed) — it set the flag but its `refreshTargetBlocks` eventflow did NOT refresh the block (updateRecord lacks the `afterSuccess` refresh cycle; only RecordTriggerWorkflowActionModel has it). The workflow binding + afterSuccess + eventflow are not exposed by public flow-surfaces writes (configure/add-record-action reject `triggerWorkflows`/`afterSuccess`), so they were patched via raw `flowModels` update on the already-registered button, mirroring the Issue button (`slybgc23q1i`) exactly — its top-level `workflowKey` column is stale (`send_po`); the live binding lives in `stepParams.customRecordTriggerWorkflowsActionSettings.triggerWorkflows.group`. **Line Items table `f3e868e9267`:** added `unit_price` col **`ul0k1lg2hkk`** + `line_total` col **`kc707cffqdw`**. **Print template:** `templates/build_po_template.py` updated (lines table now 5 cols with Unit Price + Line Total; totals box adds "Order Total (lines)" `{d.lines_total}` alongside Invoice Total/USD) → rebuilt `templates/purchase-order-template.docx` (markers verified). **Post-build fixes (2026-06-14, after user testing):** (1) the "Mark reprinted" button failed silently because `needs_reprint` had been scrubbed from procurement's PO **update** whitelist — **re-added `needs_reprint` to update only** (button writes it directly as Pat; verified a procurement-role write now succeeds). `lines_total` stays out of all write whitelists. (2) **Print template real fix:** the printingTemplates record `kkooshlz8rf` references the **hashed** file `178097646034702149965747981053.docx` (NOT `po-template-mvp9e.docx` as the old doc claimed) — the new docx is now copied over **that** hashed file (markers verified live; old hashed file backed up to `backups/`). Re-print to pick it up (Carbone re-reads the file). **One user step remains:** **drive the Issue button** to confirm the all-priced gate live (custom-action — not headless-drivable). **Cleanup:** one labelled orphan test PR `370020208672768` ("TEST budget guard") + the staged walkthrough PO `PO-TEST-WALK` (`370025982132227`, draft, priced+unpriced lines) left for the next fresh-slate wipe.
- **MVP9d — DONE + VERIFIED 2026-06-09.** Complete workflow (`qh7b3hc5q1r`, D34 active ver `368971625791488`), broadened Close (`f8gpu17s6hq`), Close Guard (`b6brl8r9c58`), and the two immutability guards (`xvcsdv07c5j` PO header, `f3dkb37te22` po_lines) are live + enabled. **Complete button** `e60dce0bb2d` on PO detail block `g9xffr68350`. **End-to-end verification passed 2026-06-09** — all 8 cases green: C1 (received+invoice+USD→completed), C2 (Complete button hidden on non-received), D34a (received, no invoice→blocked/hidden), D34b (received, invoice, total_usd=0→server guard rejects), D35 (Close Guard blocks Close on `received`, close_reason never persists), C3 (Close on `sent`→closed), C4 (header edit on completed PO blocked), C5 (line edit on completed PO blocked). Verified by Pat-driven UI clicks against **API-staged throwaway fixtures** PR-26-0008..0011 / PO-26-T1..T4 (see cleanup note below). MVP9d milestone committed.
- **Test-fixture cleanup DONE 2026-06-12 (D41):** PR-26-0008..0011 + PO-26-T1..T4 (+2 lines) deleted with the three guards temporarily disabled; guards re-enabled and re-verified blocking afterwards (Guard A on an approved PR, PO + line immutability on a completed PO). Snapshots in `backups/fixtures-*-20260612.json`. PR-26-0014 "olvire custom" kept (user choice, still `pending_purchasing_review`).
- **MVP9e — DONE + VERIFIED 2026-06-09.** PO template printing: Carbone template record `printingTemplates:kkooshlz8rf` ("Purchase Order") → `storage/print-templates/po-template-mvp9e.docx`, Havenbeheer-branded (logo + contact letterhead, green theme). Print button **`c579329db0d`** on PO detail block `g9xffr68350` bound to it (`convertedToPDF:true`). Repo source: `templates/build_po_template.py` + `purchase-order-template.docx` + `main-logo.png`. User confirmed the live PDF (PO-26-0006) looks great; P2 (internal_notes never prints) holds. Status / expected_delivery / ref-PR were dropped from the final design per user. Gotchas saved to auto-memory `feedback_template_print_{authoring,select_label,carbone_date_format}`. The PO detail block's Close button `lylrxwl1b3g` is gone (user removed it earlier on purpose).
- **PO surface drift (FYI):** the user rebuilt the PO detail block — the doc's Close button `lylrxwl1b3g` is gone (removed on purpose). (MVP9e Print button is now configured — see above.)
- **Doc-lag corrected this session:** Send-PO active `367086330314752`, Receiving-recompute active `368710576439296` (see sections + Stale IDs). **PR-Approval node/surface tables REFRESHED 2026-06-13** against the live active `369536223739904` (31 nodes; surfaces + assignees re-inspected) — no longer lag. Stray disabled PR-Approval ver `368062113775616` not re-confirmed.
- **MVP7 was descoped.** Only `suppliers` was built; `supplier_issues` and `supplier_evaluations` are postponed (D26). Don't assume they exist.
- ~~Supplier UI page UID~~ — **CAPTURED 2026-06-13:** Suppliers flowPage `gbg516xq3ie` (route `366222348582912`). See "Suppliers page (MVP7)" under UI. Block-level uids still TBD.
- **MVP8 ACL note — SUPERSEDED 2026-06-11 (D38):** field gating is now enforced via ACL whitelists, not just form `readPretty`. See the "Roles & ACL" section above (authoritative); the old strategy table (member/director `view/update/create`) no longer applies.
- **Role hardening COMPLETE (2026-06-12).** All HANDOFF.md phases closed (D38–D41); HANDOFF.md deleted. Follow-up status after the late session (D42):
  - ~~`po_lines` titleField~~ — **DONE 2026-06-12** (→ `description`).
  - ~~D39 cancel question~~ — **RESOLVED: Cancel retired (D42)**, button + both workflows deleted. Scope 2's editable window staying `info_requested`-only is accepted.
  - ~~Smoke test~~ — **PASSED** (user-confirmed first PR submission post-D41 trigger trim).
  - ~~Board Return action~~ — **RESOLVED: intentionally absent** (user-confirmed; Approve+Reject only).
  - ~~Dangling scopeId 363334209503233~~ — **NOT A BUG**: it's the built-in "All records" scope in `dataSourcesRolesResourcesScopes` (separate table from numbered `rolesResourcesScopes`). No action.
  - ~~Scopes 3, 6–9~~ — **DONE 2026-06-13 (D43)**: deleted the 5 inert scope rows (3,6,7,8,9) + their 8 orphaned `dataSourcesRolesResourcesActions` rows (all 5 parents were already gone → fully inert; snake_case FK columns would 400 if ever bound). User chose delete over camelCase-fix. Live-verified: 4 scopes remain (2,4,5,10), 0 orphans, 0 dangling references. Backup `backups/acl-scopes-3-6-9-DELETED-20260613.json`.
  - ~~Workflow executions retention~~ — **DONE 2026-06-13 (D44)**: set `options.deleteExecutionOnStatus:[1]` (auto-delete *resolved/success* only; keep `-1` blocks + `-2` errors for debugging) on all 6 interception guards (`496ookqmg01`, `vgv8hcrtjvx`, `mhfp4d15uee`, `xvcsdv07c5j`, `f3dkb37te22`, `b6brl8r9c58`) via `resource update` on each current version (no revision needed — `options` is editable in place). Pruned the 71 existing resolved guard executions; 32 remain (27 blocks + 5 errors). Manifest backup `backups/guard-resolved-executions-DELETED-20260613.json`.
